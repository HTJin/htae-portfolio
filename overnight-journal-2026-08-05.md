# Dev journal — 2026-08-05

One line per task as it completes.

- **Cycle 1 · T1** — `route.js` project stops now carry every screenshot as `images[]` (plus `site`), not just `screenshots[0]`. — `8883aaa`
- **Cycle 1 · T2** — New `ProjectShots`: browser-chrome frame at the captures' native 2:1 with `object-contain`, 900ms cross-fade on a 4.2s timer, dots, hover/focus pause, reduced-motion fallback. — `8883aaa`
- **Cycle 1 · T3** — `StopCard` rebuilt: interstate exit shield, media|prose split at `lg`, projects up from the dash, and re-bounded so it clears the rear-view mirror. — `8883aaa`
- **Cycle 1 · T4** — Cockpit rebuilt on real driver geometry: wheel on the driver's axis, hooded binnacle behind it, dash grain, cowl, vents, bonnet, meaningful tell-tales, PRND, terminal centre screen, tilted pedals. — `646b717`
- **Cycle 1 · T4b** — Separate stacked cockpit for phones (cluster strip / screen / controls+pedals in one thumb row); the side-by-side dash overflowed badly at 390px. — `646b717`
- **Cycle 1 · fix** — The per-frame `style.transform` on the wheel was overwriting Tailwind's `-translate-x-1/2`, so the wheel rendered ~180px off its own axis. Centring moved to a wrapper; rotation stays on the svg. — `646b717`
- **Cycle 1 · fix** — A `display: flex` in the CSS module out-ordered Tailwind's `.hidden`, leaking the outboard vents onto the phone layout. `display` moved into the component's utility classes. — `646b717`
- **Cycle 2 · verify** — Chrome came back to the foreground, so all three parked cycle-1 checks were cleared: cold-load hydration (clean), phone title clamp (2 lines, genuinely clamped), reduced-motion carousel (autoplay off, dots still work).
- **Cycle 2 · fix** — Found `line-clamp-2` and `lg:truncate` generating zero CSS: a corrupted `.next` webpack cache was serving stale CSS. `npm run dev:fresh` restored both. Not a Tailwind/config fault.
- **Cycle 2 · T1** — New `daylight.js`: the route now passes time. Golden-hour dusk at MILE 0 → twilight → full night by the toolbox → first light at the destination. Sky, road, verge, haze, lamps and signs all follow it. — `dd4b28b`
- **Cycle 2 · T2** — Exit signs rebuilt as real guide signs: MUTCD exit plaque, twin posts, retroreflective flare + headlight sheen, palette-aware green. — `86d0174`
- **Cycle 2 · T3** — `/drive?exit=11` deep-links an exit; the URL tracks the exit as you travel; junk falls back to MILE 0. — `0d3e493`
- **Cycle 3 · T1** — Trip computer now reads the year, not just miles: 2016 at school through 2025 at StarPlus, then `NOW`. Projects/toolbox/destination have no dates so they never get a fabricated one. — `94eea90`
- **Cycle 3 · fix** — `new Date('YYYY-MM-DD').getFullYear()` reads UTC midnight in local time, so Jan 1st dates report the previous year. The StarPlus UI/UX role ("Jan 2024 - Oct 2024") was showing 2023. Year is now read off the string. — `94eea90`
- **Cycle 3 · T2** — Each leg has its own roadside: guardrail + thinned lamps on the scenic overlook, lights thinned further across the sabbatical. Logic proven by SSR probe; pixels parked as Needs testing. — `c209b57`
- **Cycle 3 · env** — Chrome can no longer reach the dev server on any host/port (curl can). Switched to SSR probes for real-execution verification.
- **Cycle 4 · T1** — Swept every date call site in `src/`. The classic site is clean: `FormattedDate` already pins `timeZone: 'UTC'`. `route.js` was the only affected place and it was fixed in cycle 3. No change needed.
- **Cycle 4 · T2** — Found `/drive` shipping **two** canonical tags, the first pointing at the homepage, plus duplicate `og:url`/`og:title`. Fix belongs in `_app.jsx`, outside write scope → parked as Needs human with an exact patch.
- **Cycle 4 · T3** — The crawlable itinerary now groups stops by leg (h1→h2→h3) and carries a `<time>` year for each of the ten dated stops; the eleven undated ones carry none. — `e799e92`
- **Cycle 5 · T1** — Cockpit now reads properly to screen readers: 10/11 controls named (the 11th self-names), and the gauges, gear selector and trip screen removed from the accessibility tree. — `5fcf996`
- **Cycle 5 · T2** — `/drive` was serving two `<h1>`s; the transient ignition splash is now an `<h2>`. — `5fcf996`
- **Cycle 5 · T3** — `/drive` is absent from `public/sitemap.xml`. Out of write scope → Needs human with an exact patch; it compounds with the cycle-4 canonical defect.
- **Cycle 5 · env** — Chrome still unreachable (3rd cycle). Dev server also hung mid-cycle on a poisoned `.next`; `dev:fresh` cleared it.
- **Cycle 6 · unblock** — The network blocker cleared: Chrome reached a production build on :3008 and drove the route. Cycles 3-5 had been pixel-blind.
- **Cycle 6 · verify** — Cleared the cycle-3 parked item: the overlook guardrail renders as one continuous seam-free ribbon, lamps thin on the overlook and thin further across the sabbatical, no guardrail elsewhere. Frame-rate still unmeasurable (rAF pauses in a background tab).
- **Cycle 6 · false alarm** — `?exit=` looked broken on `next dev`; the real cause was that React never hydrated (body.overflow unset, chunks all 200, no console error). Same commit on a production build worked perfectly. No code defect. Now guardrail 23.
- **Cycle 7 · T1** — Oncoming headlights on the far carriageway: deterministic, pre-allocated, clamped frame delta, palette-tinted. Proven by a controlled pixel diff (12,564 samples changed in the approach path while parked). — `ff1f8d9`
- **Cycle 7 · T2** — Mile markers every half-leg. First pass rendered ~2x1 pixels and was invisible; enlarged and set outboard until they read as green plates distinct from the delineator posts. — `ff1f8d9`
- **Cycle 8 · T1** — A returning visitor is now _offered_ their furthest exit on the ignition screen ("Resume · EXIT 13") with "Forget my progress" beside it — never auto-jumped. `?exit=` always wins. Storage is versioned, id-validated, forward-only and fully wrapped. — `4ebe999`
- **Cycle 9 · T1** — Phone-width regression sweep of the three stated priorities: clean, nothing changed. A 26px "overlap" of the arrival panel into the dash was a frozen framer-motion transform, not a layout fault — deliberately not "fixed".
- **Cycle 9 · T2** — Opt-in engine audio: synthesised, off by default, context built only inside the click handler, never persisted, closed on unmount. Verified by spying on the AudioContext constructor. — `655a3ce`
- **Cycle 9 · fix** — The audio toggle lit up even when the browser refused to resume. `enable()` now reports whether audio actually started, so the button can't claim "on" over silence. — `655a3ce`
- **Cycle 10 · T1** — Route map now behaves like the modal it declares: focus moves in on open, Tab/Shift+Tab cycle inside, Escape still closes. It previously left focus outside a dialog holding 23 tabbable elements. — `09fe15d`
- **Cycle 10 · T2** — Found the user's priority (c) only half-fixed: the CLASSIC site still crops every project screenshot (10.1% of width on average) and only cycles on click. Out of write scope → Needs human with an exact patch.
- **Cycle 11 · T1** — Caught a regression this branch introduced in cycle 7: oncoming traffic ignored `prefers-reduced-motion`, sliding toward a parked viewer who had asked for less motion. Now not drawn under the preference. — `eef571b`
- **Cycle 11 · T2** — Swept the whole reduced-motion contract end to end for the first time: traffic, throttle-jumps, panel fade variant and carousel autoplay all hold.
- **Cycle 12 · T1/T2** — Landscape phone: the arrival panel sat 71px behind the dash and glass was down to 45.6%. The dash height was written twice in different units; both now consume one `clamp(190px,36%,48%)`. Overlap 71px→0, glass →50.8%. — `c71b624`
- **Cycle 12 · investigated** — Two alarms dismissed rather than "fixed": no focus-visibility defect (readings came from programmatic focus, which doesn't match `:focus-visible`), and the "control below the viewport" was the sr-only itinerary's links.
- **Cycle 13 · owner request** — Removed the oncoming traffic entirely; it doesn't belong on a site that represents a person. Recorded as standing law so no future cycle re-adds it, and closed S13b (haze) for the same reason. — `d76e0bd`
- **Cycle 13 · owner request** — The car drove astride the centre line, and steering re-centred it there. Camera now sits at `LANE_OFFSET + sim.x` (2.7m = middle of the right lane); `sim.x` is drift _within_ the lane, so steering returns you to your lane. — `d76e0bd`
- **Cycle 14 · check** — Verified cycle 13's camera change broke nothing: the exit-sign overlay still lands correctly at the roadside, and the first-run flow (start → hold accelerator → arrive) works end to end.
- **Cycle 14 · T1** — The destination read like any other stop: the email and "back to the classic site" had identical weight. Email is now primary, the exit link quiet, and a trip summary derived entirely from the content (2016 / 9 / 8 / 2.7) sits above them. — `b6f444f`
- **Cycle 15 · T1** — `world.project()` was dead code whose doc claimed it was the single source of truth for all on-screen placement; three sites duplicated it instead. Two now call it; the hot loop stays inlined with a comment saying why. Proven pixel-identical by a stash/rebuild A/B: 0 of 1,992,704 pixels differ. — `e23a9db`
- **Cycle 16 · T1** — The app contradicted itself one click apart: _"Resume · EXIT 13"_, then a route map saying exits 01-12 were never driven. Saved progress is arrival-only and forward-only, so the resume path now restores the whole history behind it — and only the resume path, because a `?exit=` link proves a click, not a drive. — `f0b7c6c`
- **Cycle 16 · recorded, not built** — Parked, the canvas repaints every frame for a provably identical image. Logged as backlog S16a rather than shipped: the win can't be measured with rAF suspended, and an unmeasurable optimisation in a hot path is exactly what guardrail 9 forbids.
- **Cycle 17 · T1** — Twenty-one exits shared one tab title, one bookmark and one history entry — and Next's route announcer was reading that same string assertively on every departure. The title now names the exit, which fixes the tab and the announcement together; the crawler's `<title>` is untouched. — `aafb4d9`
- **Cycle 17 · T2** — The arrival panel was labelled `aria-live` but remounts on every arrival, so it announced nothing and vanished entirely while driving. A permanently mounted `role="status"` region does it properly; verified by node identity across three arrivals. — `aafb4d9`
- **Cycle 18 · T1** — The road was drawn from an eye at 50% of the viewport while the cockpit was laid out around one at 32% — the wheel sat 345px left of the driver's own eyeline. Proved where the vanishing point actually is with a Node probe, corrected the false comment that justified the old framing, and made the middle grid column the steering column so the wheel is centred by construction. The door card fills what that leaves beside you. — `4d24fd9`
- **Cycle 19 · verification** — The window would not resize, so `/drive` was measured inside sized same-origin iframes instead (proved to be real viewports first). The re-centred dash holds at 1100x800 and the phone layout is provably untouched at 390x844 and 844x390. No defect found; the `xl` threshold now carries the arithmetic that produced it. — `72ccf3f`
- **Cycle 20 · T1** — Frame rate measured at last: driving holds 29.9-30.0fps across three samples while _parked_ manages only 17-22, so the ~30 ceiling is this environment (28fps for an empty loop) and the drive loop is not the limiting factor.
- **Cycle 20 · T2** — Route map focus return observed end to end: trigger -> dialog panel -> Escape -> back on the trigger. Escape still reaches the global handler.
- **Cycle 20 · T3** — The engine audio was never broken, only untestable: a scripted click grants no user activation. With a real click the context runs, master gain reaches 0.09, and the oscillators climb 43.8 -> 71.6Hz with the revs, the square exactly an octave above, tyre noise building with speed.
- **Cycle 21 · T1** — The road repainted a 3840x1790 canvas every frame while parked to produce an identical image (130 paints / 130 frames, measured). Now skips when travel, lateral position and camera are all unchanged: 0 paints / 140 frames parked, driving untouched, resize still repaints, and 0 of 4,096,000 pixels differ. — `574ff15`
- **Cycle 22 · T1** — The carousel cross-fades with opacity, which does not hide anything from a screen reader - so all four captures were announced at once. Only the visible frame is exposed now, and it follows the cross-fade. — `2f62f6d`
- **Cycle 22 · T2** — The carousel dots were 6x6px targets against a 24x24 minimum, inside a panel that shows on phones. Now 24x24 boxes around unchanged 6px pills, no overlap. Guardrail 69 said the dots must not move; that was unsatisfiable, so it was amended in the open rather than broken quietly. — `2f62f6d`
- **Cycle 23 · T1** — The destination's last two actions were sliced through the middle on a phone (12px hidden). The trip summary now runs four across instead of stacking 2x2, which halves its height; desktop verified unchanged. 375x667 still scrolls and is recorded as a limit, not fixed by cutting content. — `06f6e26`
- **Cycle 23 · T2** — The phone control cluster overflowed its row by ~11px, stranding the audio toggle alone in the corner. Reclaimed from padding, gaps and tracking below sm - one row at 390 and 360, no label or target size changed. — `06f6e26`
- **Cycle 24 · T1** — The bonnet, dash reflection and wipers were pinned at fixed percentages while the dash is a clamp - so below ~528px tall the dash grew over them and on a landscape phone the bonnet vanished entirely (0 of 23px). The dash height is now one `--dash` token read by all three consumers; desktop verified pixel-identical, landscape phone 0 -> 24 of 26px. — `bf95af8`
- **Cycle 25 · T1** — On a landscape phone the trip computer had 20px of box for 54px of content and, being overflow:visible, painted over the controls instead of clipping - the exit line and progress bar were squashed to height 0. Rebalanced the 190px dash budget on short viewports only (52/68/48 from 74/20/62); portrait and desktop measured identical. — `e9cfe30`
- **Cycle 26 · T1** — The ignition splash overflowed short landscape phones for _returning_ visitors (the resume controls add ~50px), pushing the only way out of drive mode off the bottom - at 568x320 by 74px. Made it scrollable with the centring moved to an inner `m-auto` wrapper, since a centred flex child that overflows can't be scrolled back to, plus tightened below 430px. — `d2d0484`
- **Cycle 27 · T1** — The route map highlighted the current exit but always opened at MILE 0 - on a landscape phone that is 3 visible rows of 21 with the current one 803px down. It now opens centred on where you are; MILE 0 still opens at the top. A first attempt used `offsetTop` and was 111px out because the scroller isn't positioned - caught by two checks disagreeing, fixed with rect deltas. — `89803a6`
- **Cycle 28 · sweep** — Opened all 21 exits for the first time: every card renders, counters correct, all 28 screenshots load, zero console errors. Clean.
- **Cycle 28 · T1** — The stops with pictures got 925px and the stops with prose got 704px, so the sabbatical hid 40.9% of itself on a 1440x900 laptop. Text stops now get the same width and flow in two columns - fold fixed for two of three and line length improved from ~100 to ~63 characters. A first build made it worse (break-inside-avoid on every child stops columns balancing); the re-sweep caught it. — `6dcdd2e`
- **Cycle 29 · verification** — Re-ran the whole reduced-motion contract against the current build, eight cycles after it was last exercised: carousel frozen, panel opacity-only, Next teleports in 102ms vs 9,887ms with motion allowed, 0 idle repaints. Each checked against a control run so a pass can't mean the harness did nothing. No defect; no code changed.
- **Cycle 30 · T1** — The exit sign's fade-in was dead code: its 420m window is 1.91x the 220m leg, so opacity computed to 1.000 at departure and the sign popped in at full brightness. Window and fade now derive from `LEG_LENGTH`; measured 1.000 -> 0.004 at departure, full by 4.8s of a 9.9s leg, flare starting at 0 instead of 0.227. — `f299914`
- **Cycle 31 · audit** — `Sky.jsx` and `daylight.js` reviewed - shared palette safe, stars deterministic, and the reduced-motion CSS covers all four animations. The last two unaudited drive files are now done.
- **Cycle 31 · T1** — Full night was anchored at 0.92 and labelled 'the toolbox', but the toolbox is 0.95 - so the darkest point fell mid-leg and EXIT 14 measured a brighter moon than EXIT 19. Anchor now derived from the stop; toolbox star/moon 0.850/0.888 -> 1.0/1.0 with the destination untouched. — `f6c267f`
- **Cycle 32 · T1** — Enumerated every numeric constant in `drive/` and asked which must agree with something it cannot see. Three did: Sky re-typed the palette's 360-step quantisation, MARKER_SPACING typed 110 for 'half a leg', and GEAR_RATIOS ended at a literal 42 for MAX_SPEED. All now derived; proven a no-op at 0 of 4,096,000 pixels different across two exits. — `57cd6cc`
- **Cycle 33 · check** — No subscriber leak: 13 per-frame listeners at MILE 0 and still exactly 13 after twelve stop changes, despite `drive` changing identity on every arrival.
- **Cycle 33 · T1** — The sr-only itinerary is clipped but still in the tab order, so Tab spent 25 presses on invisible links - focus painted on clipped-away content - before any visible control. Added a skip link: 25 presses -> 1, with the itinerary untouched for screen readers. Proving it needed real key presses; programmatic focus never matches `:focus` on an unfocused document. — `de9a7da`
- **Cycle 34 · closed** — The cycle-12 'no focus indicator' alarm is settled: 14 real Tab presses show every control with `outline: auto 1px` and `:focus-visible` true, none suppressed.
- **Cycle 34 · T1** — 13 of 14 focus stops were off-screen inside the clipped résumé. Proved by experiment that a `position: fixed` descendant cannot escape the container's clip, so the usual `focus:not-sr-only` reveal is unavailable; added an aria-hidden chip that names the focused link instead. — `ed61397`
- **Cycle 35 · verification** — Re-checked all three parked Needs-human patches against today's files after 25 cycles: all still valid, every line reference confirmed, crop figure corrected 10.1% -> 10.2%, and the duplicate canonical confirmed from the served HTML (the homepage one comes first, so /drive tells crawlers it is the homepage). My own grep briefly suggested Projects.jsx was dead code - the filter excluded the importing line.
- **Cycle 36 · check** — Resizing the window mid-drive is handled: sim kept running to arrival, canvas backing store followed, dash re-laid out, panel fully in view with zero overlap.
- **Cycle 36 · T1** — The exit sign switched from miles to _metres_ partway through the approach, in an mph/MI cockpit, and disagreed with the owner's own '1/4 mile' homepage sign. Now feet throughout (720 -> 110 FT measured, single unit), derived from METERS_PER_MILE; no fraction ladder, because below a quarter mile it could never run. — `faf5a43`
- **Cycle 37 · audit** — Checked every year against `src/content/*` in both places they appear. All ten dated stops correct in the cockpit and in the crawlable copy, `<time datetime>` matching visible text throughout, and all eleven undated stops carrying no year at all. EXIT 08 (`2024-01-01`), the entry cycle 4's timezone bug moved to 2023, still reads 2024. No defect.
- **Cycle 38 · T1** — The route map said `aria-modal` but the driving keys stayed live behind it: ArrowUp pulled the car out of the stop with the dialog still up. Keys are now inert while it is open (except Escape and M), without preventDefault so the list still scrolls, and opening it releases any held control - 22 -> 15 mph proves it. — `e06709b`
- **Cycle 38 · finding** — The missing `key` props in `_app.jsx` block four things, not one: the duplicate canonical, a drive-specific social card, `summary_large_image`, and backlog S15's structured data. Recorded rather than shipped.
- **Cycle 39 · T1** — The arrival panel capped at 928px on every screen, leaving 992px of the band empty at 1920 while the sabbatical hid 22.4%. Text stops now widen to 1216px with a third column at `2xl`: hidden 94 -> 0 and the line measure _narrows_ 412 -> 363px. A comma-operator slip briefly dropped the picture stops to 704px - caught by measuring, not by the build. — `cb525ea`
- **Cycle 40 · T1** — Project screenshots rendered at 451px - 23.7% of native - on every monitor, while 992px of the band sat unused at 1920. Now 678px (35.7%) where there is vertical room for the taller 2:1 frame; gated on height as well as width because widening at 900 tall introduced 83px of overflow. 2560x900 correctly unchanged. — `cb086c2`
- **Cycle 41 · T1** — Arriving at a project stop downloaded up to 5,306 KB of raw PNG (all frames load - they are stacked in the visible panel). Now through next/image: 131 KB of WebP, ~40x smaller, sized to the frame (470px, or 692px on a big screen). Frame box, cross-fade, contain, dots, aria-hidden and reduced motion all re-measured intact. — `1f9c323`
- **Cycle 42 · sweep** — Re-ran all 21 exits after four cycles of change: 28 images, 0 broken, all through the optimiser, one frame exposed each, zero console errors. `/drive` cold-loads at 208 KB.
- **Cycle 42 · correction** — Cycle 38 said the missing `key` props block four things; measured tag by tag, `next/head` auto-dedupes `<meta name>`, so the drive page's description and twitter:title are already correct. Only the canonical, the `og:` tags and S15's JSON-LD actually need it.
- **Cycle 43 · audit** — Checked whether cycle 41's 40x byte saving cost image quality. It did not: currentSrc is w=1080 into a 451px slot (~2.4x on a DPR-2 display), and the alarming `naturalWidth: 470` is just srcset density-correction, confirmed by re-decoding the URL to 1080x552.
- **Cycle 43 · not changed** — Measured q=75 against near-lossless: mean channel error 1.73/255, 1.5% of pixels over 8. q=90 would improve that to 1.25 for +76% bytes. Left alone - raising it on 'probably looks better' would be a guess.
- **Cycle 44 · checks** — Browser zoom to 200% (720x450) and forced light mode both hold: no overlap, nothing off-screen, scene covers the white body.
- **Cycle 44 · T1** — With JavaScript off, /drive was a dead end - a splash with an inert button and the whole résumé present but clipped to nothing, with no `<noscript>` anywhere in the app. Added one pointing at the classic site, which does render without JS. Verified by disabling scripting for real via an unsandboxed-script iframe; costs the JS path zero bytes of layout and no hydration warnings. — `a779878`
- **Cycle 45 · checks** — Route boundaries audited: junk/out-of-range ?exit values fall back to MILE 0, and progress.js validates stored id against the index. Both clean.
- **Cycle 45 · T1** — At the destination the GO pedal stayed fully lit, depressed under the press and did nothing, while NEXT beside it correctly greyed out. Now disabled and named for the reason. Brake/back/map stay live; exit 10 still drives 0->27 MPH. — `c2338c8`
- **Cycle 45 · T2** — Found while pre-morteming T1: arriving at the destination with focus on NEXT dumped document.activeElement to <body> at t=13.0s - already shipped, not new. Focus now moves to #drive-controls, and only when that is what happened. Throttle released too, since a pedal disabled mid-press never gets its pointerup. — `c2338c8`
- **Cycle 46 · audit** — Four angles, nothing to ship: ignition splash, the MILE 0 mirror (reads "Open road"), reduced motion re-verified against cycle 45's new pedal (teleports at 0 MPH; control drives 5->25 MPH), and wide/short viewport geometry.
- **Cycle 46 · not changed** — On a 2560x1080 a text stop widens to 1216px but a project stop stays 928px. Looks like a bug; is not. Widening makes a text stop SHORTER (340px, 0 hidden scroll at every height) but a picture stop taller: forcing it costs 10px of hidden scroll at 1080 and 150px at 800. Cycle 39's height guard earns its place.
- **Cycle 47 · audit** — Five angles, nothing to ship: the carousel already pauses on hover and focus; browser history is not polluted (history.length unchanged, Back returns to /); drive.module.css is 25 defined / 25 used with no dead rules and no dangling refs; the served HTML carries 21 h3 for 21 stops across 6 legs.
- **Cycle 47 · signal** — Second cycle running with no commit to src/. Logged an exclusion list at the top of the task file so later passes widen the angle instead of re-probing, and noted that the best remaining work (Projects.jsx on the classic site) is parked in Needs human, outside this run's write scope.
- **Cycle 48 · check** — Stop titles are clipped to one line from lg up; swept all 21 stops at 1440x900, 1280x800 and 1024x800. 0 of 21 truncated.
- **Cycle 48 · T1** — EXIT 12's description ended in a raw, unclickable URL. Now a link, with the owner's copy passed through character-for-character (src/lib/projects.js is read-only) and built as React elements, not HTML. Regex tested against trailing punctuation cases that do not ship; 21/21 stops render with zero anchors added elsewhere; the sr-only itinerary deliberately stays plain so no tab stop returns to it. — `4a54a2e`
- **Cycle 49 · T1** — Swept all 21 panels for hidden content. EXIT 4 (the sabbatical, 2,023 chars - the longest entry on the resume) was the only stop with anything below the fold: 22% hidden at 1440x900, 34% at 1280x800, but 0 at 1920. Cycle 39's wide layout was gated at 2xl; moved it and the third column to xl. Zero regressions across all 21 stops at both sizes; total hidden at 1440x900 is now 0; the measure narrows 412px -> 359-363px. — `9a2a721`
- **Cycle 50 · audit** — Cycle 49's change re-checked where wide meets short (never tested there): 1366x768 hides 52px vs 136px before, 1280x720 76px vs 160px. No regression.
- **Cycle 50 · T1** — The panel band's top was a flat 14% while the mirror's drop is a fixed 50px, so they crossed below 769px tall: gap 0 at 1366x768, -3 at 1280x720, -25 on a landscape phone, with the chip visibly sliced by the panel border. The mirror cannot move (headliner is right above it), so the floor went on the panel: max(14%,calc(7.5%+58px)). All 21 stops now clear by >=8px; tall viewports byte-identical. Costs 8-11px of reading room at 720-768, which is recorded as a cost. — `6544a7a`
- **Cycle 51 · audit** — Cycle 50's 58px mirror floor re-checked on phones, where a long mirror title could have wrapped and broken it: the chip stays 38px at all 21 stops at both 390x844 and 844x390 (titles truncate), min gap +16 and +8, no negatives. Route map geometry also clean at every size.
- **Cycle 51 · T1** — The route map marked the current exit in colour alone - no aria-current anywhere - so a screen reader met 21 near-identical buttons. Added aria-current="location" (ARIA's token for a place on a map). Exactly one row marked at exits 0/12/20, it is the same element as the visual highlight, and the styling is unchanged. — `d88f5cd`
- **Cycle 52 · audit** — Hunted the whole "state in colour alone" class after cycle 51's find: clean everywhere else. Canvas, sky, interior, exit sign, cluster, gear block and trip computer are all deliberately aria-hidden with the meaning carried in text elsewhere. Canvas DPR handling also correct.
- **Cycle 52 · limitation** — Frame rate is NOT measurable in this environment. Driving sampled 16.1 fps, which looks catastrophic - but a bare blank page runs at 15.0 fps and parked drive mode at 15.2, with the window foregrounded. Drive mode is at the empty-page ceiling. Recorded at the top of the task file so no later cycle reports the harness as a regression.
- **Cycle 53 · T1** — No visibilitychange handling anywhere: disable() only ramps gain, nothing suspends, and update() rides on rAF - so a hidden tab left the oscillators holding their last revs with the gain still up. The context now suspends on hide and resumes on show, gated on the sound being on. Verified by patching AudioContext.prototype and driving a real visibilitychange: suspend on hide, resume on show, zero contexts and zero calls when sound was never enabled, listener removed when toggled off. Whether Chrome suspends hidden tabs itself could NOT be tested here - the fix is a no-op if it does. — `78d7f0f`
- **Cycle 54 · T1** — My own regression from cycle 53: the visibility handler ignored unpause()'s return value, so a refused resume left the toggle claiming sound was on over a suspended context. Proved it by patching AudioContext.prototype.resume to reject. Now only `false` turns the toggle off (undefined = no engine). Verified refusal, recovery, success and sound-never-on. Cycle 53 passed because the happy path passes either way. — `e5fce6a`
- **Cycle 55 · audit** — Applied cycle 54's lesson as a lens: hunted unhandled failure paths. router.replace has no catch and Next rejects on cancelled route changes, but 14 rapid Back/Next clicks produced zero errors, zero unhandled rejections and a coherent state. Chaos during motion also clean, and the car recovers - stopping between exits with no panel is correct, not a desync (my check was wrong, not the page).
- **Cycle 56 · audit** — Checked every outbound link for the first time. resume.pdf is genuine (decompressed and read; matches experience.js), 18 of 21 URLs resolve, LinkedIn's 999 is anti-bot not dead.
- **Cycle 56 · Needs human** — Solar Power Indy (EXIT 11) has TWO dead buttons: gosolarindy.energy is NXDOMAIN (nslookup 'Non-existent domain', curl exit 6, www and http variants same, control vercel.com 200) and github.com/HTJin/solar-questions is 404 in 0.39s while every other HTJin repo returns 200. Both live in src/lib/projects.js, which is read-only - parked, not guessed at.
- **Cycle 57 · environment** — The session opened on six orphaned `next dev` servers (3111-3116) plus a `next start`, all from the previous session and all sharing `.next`: guardrail 6 running six times over, with `/drive` serving a flat **500**. Killed them, wiped `.next`, rebuilt clean. Recorded at the top of cycle 57 so a later pass checks for orphans before suspecting the code.
- **Cycle 57 · T1** — The owner asked for a real highway, and the road was not one: a **dashed** centre line at x=0 is the marking for a road you may overtake into oncoming traffic on, and every stop was just a point on it the car halted at. Now `ROAD_HALF` is `CARRIAGEWAY` (the old name claimed "from the centre line"), the left lane is a **median with a barrier** and an empty carriageway beyond it, and every stop is a real interchange — `rampAt(s)` peels a deceleration ramp off the mainline over the 88m before each stop and merges an acceleration ramp back over the 88m after, one smoothstepped shape for both. `RAMP_LENGTH` is derived from `LEG_LENGTH`, because the entrance ramp out of one exit has to finish before the exit ramp into the next one starts. The ramp is added **outside `sim.x`** in `cameraX()`, so the owner's lane-centring rule survives. Proven at a stop by pixels — mainline, edge line, gore, ramp edge line, ramp. The taper **in motion** is NOT proven: the Chrome window is minimised, rAF is paused (guardrail 24 — the first probe hung CDP the full 45s), and every position reachable without the sim loop is a stop, where the ramp is at full offset by definition. Left in Needs testing. — `a511ce0`

- **Cycle 58 - verification** - Cycle 57's ramp taper is proven in motion after all. Guardrail 24 had been treated as "wait for a human to un-minimise Chrome"; it is not. Patching `requestAnimationFrame` inside a same-origin iframe _before hydration_ (cycle 29's timing) turns the drive loop into something I can crank by hand at 16.7ms a frame, and the live sim comes off the canvas's React fiber in one hop. Drove 557 frames, zero errors: `ramp` is exactly 0 from travel 308 to 351.6 - the predicted mainline window to the metre - 8.2 at both stops, symmetric about the leg, largest slope discontinuity 0.004. Recorded as a reusable probe; it retires a standing excuse.
- **Cycle 58 - T1** - The dash was symmetric at xl, so the wheel sat on the screen centre line with a 503px empty slab beside the driver. Cycle 18 had centred it on sound geometry - the camera is the driver's eye, so what is in front of the driver lands in the middle of the image - but that renders a car with the driver sitting in the middle of it, and the owner said so twice. Columns now 0.5fr / wheel / 1.5fr: wheel 358px left of centre at 1920 down to 221 at 1280. `lg` still measures the exact 153px cycle 18 recorded, which is how I know I did not disturb it. Guardrail 50 amended in the open. - `f1c5b4f`
- **Cycle 58 - T2/T3** - The pedals took three goes: far right (wrong side of the car), inboard as a flow child (shoved the HUD 330px right), and finally pinned out of the flow at the bottom left of the console column - right of the wheel, costing the console nothing. Two things only measurement caught: `.footwell` sets `position: relative` for its shadow and beats Tailwind's `absolute` on source order, silently dropping the pedals at the top of the dash; and below xl the centred button row ran under the pedals, GO overlapping "Back" at 1100x800. Overlap is now asserted pairwise at five widths. - `6d2e3aa`, `5ee5782`
- **Cycle 58 - T4/T5** - The highway was a single 5.5m lane each way and the exit cleared it by 2.7m. Now two lanes with a broken lane line drawn from `LANES`, the ramp 14m clear, `LEG_LENGTH` 220 -> 340 so the ramp could grow 88 -> 136m, and the exit descends: `rampDropAt` shares one `rampProgress` with `rampAt`, proven by `drop / ramp` holding constant to 2e-17 over 617 frames. The drop is capped at `CAM_HEIGHT * 0.85` and the cap is a renderer limit, not taste - at 6.5m the entire mainline lifted above the horizon and painted across the sky, because flat ribbons with no depth buffer cannot show an embankment. Filed S102 rather than bodge it. - `694c4f6`
- **Cycle 58 - T6** - Cycles 39/49 measured multi-column text stops as a win and were right about _items_; prose is the opposite, and the sabbatical - the longest and most personal entry on the resume - was set in three short columns you had to track bottom-to-top every few lines. Split now by content rather than width: paragraphs-only stops get one column with the measure bounded in `ch`. - `4195b71`
- **Cycle 58 - not a defect** - The "gap spilling out the road on the right side" was my own harness: a 1440px iframe overlaid on the live 1920px page, with the page showing through beside it. Removed, reloaded, re-measured - full-width dash, no horizontal scroll. Nothing changed, because nothing was wrong.
- **Cycle 58 - self-inflicted** - Mangled `useDrive.js` with a PowerShell `Get-Content`/`Set-Content` round-trip: WinPS 5.1 reads as ANSI, so every em-dash came back as mojibake. Caught it in the tool's own diff, reverted with `git checkout`, redid the edit properly. Source files get edited with the editor, never round-tripped through the shell.

- **Cycle 59 - T1** - S102, the embankment. The cap on the ramp's descent came off by fixing its cause: a horizontal surface above your eye cannot be seen, a vertical face can. So the flat road surfaces are drawn inside a clip to below the eyeline while standing objects - lamp masts, the median barrier, the embankment face - stay outside it, and a new `embankment()` fills the gap with its top edge at `point.y` and its bottom at `point.yRamp`, the same two values the two roads are painted from. Drop 1.15 -> 3m. **I tried 7.5m first and rejected it by looking at it:** with the camera ~6m below and ~17m to the side of the highway the embankment's near field is off-screen left and its polygon sweeps in as a black wedge over the sky - this projection running out of road, not a constant to nudge. Filed S104 for the real 3D clipping that would be needed. Crest case re-checked at travel 150.6 per my own pre-mortem, since a baseline measurement had shown 26,683 road pixels already painting above the eyeline before I touched anything: mainline correct, no artefacts. Three legs pumped, arrivals exact at 340/680/1020, `drop / ramp` constant to 5.6e-17. - `8264530`

- **Cycle 60 - T1** - Drove the pedals with touch for the first time and found two real defects, both in `onPointerDown`. **A disabled button is not inert:** Chrome dispatches pointer events to it, so pressing the greyed-out GO at the destination ran the handler and set the throttle. I did not infer that from synthetic dispatch - I settled it with a **real click** on a disabled button next to an enabled control that proved the click landed. And **`setPointerCapture` could eat the press**: `?.` guards a missing method, never a throw, and when it threw, `onPress()` never ran and the pedal did nothing at all. Both fixed; re-verified across seven cases with zero uncaught errors, including that the brake stays live at the destination so cycle 45's contract is intact. - `ae31e76`
- **Cycle 60 - method** - Two of my first-pass "failures" were the harness, not the product, and saying so mattered as much as the fixes. React synthesises `onPointerLeave` from `pointerout`, so dispatching a raw `pointerleave` proved nothing; and React's scheduler is not driven by rAF, so pumping frames does not flush a state update - the first destination check read `disabled: false` purely because the re-render had not happened. With a real wait it reads `true`. A harness that lies looks exactly like a product that is broken.
- **Cycle 60 - non-finding** - Chased mojibake in `Dashboard.jsx` that was not there: Windows PowerShell 5.1 reads BOM-less UTF-8 as ANSI, so `Get-Content` _displays_ em-dashes as garbage in files the editor wrote correctly. Files PowerShell itself writes carry a BOM and read back clean, which is why the earlier check passed. Verify encoding with the editor, not the shell.

- **Cycle 61 - T1** - Measured the centre display before changing it: **720 x 116, an aspect ratio of 6.2:1**. Researched what cars actually use - production centre displays are ~10-12.3 inches on a **16:9** panel (~1.78:1), and the pillar-to-pillar outliers like the 56-inch Hyperscreen are several displays side by side rather than one strip, so they are not a precedent. The box is now `aspect-video` taking its width from its height: 1.77-1.78:1 at every width tested. Two knock-ons taken deliberately: the centre stack is capped at 24rem, because a stack should not be wider than the display it houses, and the footwell goes back into normal flow - safe only because the stack is capped, since an in-flow footwell beside an _uncapped_ stack is exactly what pushed the console across the cabin in cycle 58. - `664def6`
- **Cycle 61 - T2** - The exit panel was 76rem and three columns at xl. Cycle 49 added that third column and measured it as a win on both axes - nothing hidden, narrower measure - which was measuring the wrong thing: a 1216px panel three columns wide is a broadsheet, and it reads as one. Card down to 60rem, two columns maximum, measure now 433px or about 57 characters. - `664def6`

- **Cycle 62 - audit** - Checked the rest of the cockpit's proportions after cycle 61 fixed the "elongated hud", since the owner said "certain parts", plural. **Two expected defects were not defects.** The cluster is exactly centred on the wheel axis (offset 0 at both 1920x1080 and 1440x900) - my first reading said 40px off, and that was me measuring the two round dials while ignoring the row's third child, the gear block. And the gauges overflowing their flex row by ~9px a side is invisible, because the element that paints the binnacle is a wider parent. Both recorded so a later pass does not "fix" either.
- **Cycle 62 - finding, not shipped** - The cluster and the wheel are sized by unrelated rules: gauges in `vh`, wheel from a `vw`-based column. Measured across six viewports the ratio between them runs **0.602 to 0.831**, a spread of 0.229 - the cluster is 38% larger relative to the wheel at one window size than another, where a real car has one piece of hardware. That is the cycle-32 class of fault. **Deliberately not fixed here:** guardrail 52 forbids quietly redesigning the cockpit, and the obvious fix lands in the dash height budget that cycles 12, 25 and 44 tuned for short and landscape-phone viewports, with `aspect-square` fighting any height cap. Filed as S107 with the numbers and a safe shape for the fix.

- **Cycle 63 - S107, mechanism found, still not shipped** - The drift is not "two clamps disagree", it is three rules. `--dash` measures exactly 36% of viewport height; the wheel wrapper is `aspect-square h-full`, so **the wheel's width IS the dash height** (`dashH - 19px` in all six samples); the gauges share that height basis but carry a px cap that freezes them past ~971px tall; and the column holding both is `clamp(260px, 24vw, 400px)` - viewport **width**. **Confirmed defect from it:** at 1280x1024 the wheel overflows its column by 14% and draws **over the brake pedal** (gap -2px), and at 1024x1180 by -53px, hitting the door card too. **Tried to fix it and stopped:** `max-w-full` makes the wrapper non-square, and since the rotation is a transform about `origin-center` - the _box_ centre - content centred by `preserveAspectRatio` in a non-square box no longer shares that centre, so the wheel would **orbit instead of spin**. That would not show in a screenshot. `inset-0` fixes origin and content together but re-centres the wheel vertically out of its tuned `top-[40%]`, and no constant fraction works because the overflow ranges 0.925 to ~1.57. Needs the wheel, its column and the gauges derived from one `min(column width, dash height)` - a cockpit re-derivation, which guardrail 52 says is not a thing to do quietly. Two audit cycles on this thread is enough; it needs a decision, not a third.

- **Cycle 64 - T1** - S95 finally reached. Researched what forced colors actually overrides (box-shadow reverted, gradients collapsed to flat system colours), then audited the live page against it instead of reasoning about the CSS. Of 35 on-screen controls, 27 are plain text links that forced colors handles well and 6 carry a real border; **exactly two were painted boxes with no border - BRAKE and GO**, whose entire shape is a gradient plus a box-shadow over a transparent background. Both would have collapsed, leaving the primary driving controls as bare labels floating on the dash. `border border-transparent` fixes it and costs no layout: pedal outer rects are identical before and after at 1920, 1440 and 1280 (border-box), and re-running the audit returns an empty at-risk list. **Not claimed:** forced colors cannot be emulated through this bridge, so this is documented UA behaviour applied to a measured inventory, not the page observed in forced colors. - `dd32f65`
- **Cycle 64 - method** - My first pass over-counted badly: 29 of 35 controls had no border, which reads as alarming until you notice 27 of them are plain text links, where a missing border means nothing. Narrowing the test to "painted box AND no border" took the finding from a scary number to the two controls that actually mattered. A count is not a finding.

- **Cycle 65 - T1** - The owner found a real bug and it was the one holding everything else back: the highway had no **flank**, so there was an empty gap under it and the road appeared to snap back to grade 0 on the way up. Cause: `embankment()` was gated on **lateral separation**, but the ramp starts falling long before it moves aside, so across the first half of every taper a real height difference had nothing drawn to fill it. Now gated on **elevation difference**, with the foot clamped so it can never sit inboard of the top - which makes the quad collapse to a near-vertical face when the ramp has dropped but not yet moved, and that face _is_ the highway's flank. **This retired S104's premise:** I had capped `RAMP_DROP` at 3m and filed per-polygon near-plane clipping as the fix for the black-wedge rendering at 6.5m and 7.5m. Wrong diagnosis - the missing flank was the cause, and with it continuous, 5.5m renders cleanly with no clipping work at all. Tuning alongside: lane 3.7 -> 4.1m, ramp offset +14 -> +22m, drop 3 -> 5.5m, leg 340 -> 420m. Three legs pumped: arrivals exact, drop/ramp constant to 8.3e-17, 387 frames on open mainline, zero errors. - `b7cfa73`
- **Cycle 65 - lesson** - Twice I diagnosed "this projection cannot do it" and filed a large piece of work, when the real fault was a missing piece of geometry I had simply not drawn. A renderer limit is a strong claim; the bar for it should be higher than "my first fix looked bad".

- **Cycle 66 - sweep** - Regression sweep after cycle 65 moved `LEG_LENGTH` 24%, since several quantities derive from it. All 21 exits deep-linked: every one hydrated, every heading and `n/21` counter present, 28 of 28 images loading, no horizontal scroll anywhere. The derived route distance followed correctly - 20 x 420 / 1609.34 = 5.2 mi, and the destination reads `5.2 MI`, so cycle 32's derivation work still holds. **No defect.**
- **Cycle 66 - measured consequence** - The drive is now materially longer: 14.1s a leg at a pumped 16.7ms/frame (842 frames), so about **4.7 minutes** to hold the accelerator through the whole route, up from ~3.8 min at 340m and ~3.1 min at the original 220m. Not a defect - the direct cost of the owner's "a bit longer" - but recorded so it is a conscious trade rather than a surprise. `Next` still autopilots.
- **Cycle 66 - my own wrong reading, caught before reporting** - The first pass reported the destination trip summary as entirely missing, all four stats null. The regex was wrong and the element I had grabbed was the mirror chip rather than the stat band; dumping the panel text directly showed `5.2 MI` present and correct. A selector that finds nothing is not evidence that nothing is there - the same class of mistake as cycle 62's "cluster is 40px off" and cycle 64's "29 of 35 controls have no border".

- **Cycle 67 - Suggester** - Backlog was dry, so a fresh audit and market-research pass. Four new ideas filed: **no print stylesheet anywhere** (measured: zero `@media print` rules in any sheet, against a fixed-position cockpit that will print blank), **time-to-content** (research is blunt that hiring managers spend minutes and should not have content buried behind animation - and cycle 65's longer legs made the drive to the first stop measurably slower), plus two honest re-dos: cold page weight and heap growth.
- **Cycle 67 - two of my own measurements thrown out, not reported** - Page weight came back as 0.9 KB because cached resources report `transferSize: 0`; that number is meaningless. And the heap test was invalid because I never started the engine in the probe frame, so the car never moved and 48,000 pumped frames were no-ops. Both are filed as work to redo rather than as findings. Fourth and fifth instances this run of a measurement that looked like a result and was not.

- **Cycle 67 - T1** - S111, printing. The page had **zero** `@media print` rules and would have printed a page of dashboard with no resume on it: `body.style.overflow = 'hidden'` clips the document to one page, the canvas and cockpit are absolutely positioned scenery, and the content is a **9,854-character `.sr-only` itinerary clipped to rect(0,0,0,0)**. The fix builds nothing new - it moves the scenery aside and lets the text that is already there out of its clip. **Two things the build taught me rather than me assuming:** CSS Modules refuses any top-level selector with no local class, so both `:global(html)` and the `:global { html, body }` block form are hard build errors and the stylesheet cannot touch `body` at all; and `body`'s overflow is an inline style anyway, so it is released on `beforeprint` in `DriveScene` beside the code that set it, and restored on `afterprint`. Verified on the built page: one print block, four selectors, `.sr-only` un-hashed and `.printable` hashed and **matched against the scene root** rather than assumed; `beforeprint` moves overflow hidden -> visible and `afterprint` restores it; screen layout identical across all seven visible controls. **Not verified:** the paper itself - print media cannot be emulated here and `window.print()` opens a blocking modal that would stall the loop. - `1dde62c`

- **Cycle 68 - S114, done properly: no leak.** Redid the heap test that cycle 67 botched, this time with the engine actually started and `travel > 0` used as a **validity gate before trusting any number** - which is the check whose absence made the last attempt worthless. Drove the whole route: 20 legs, `finalTravel` 8400 = 20 x 420 exactly, 16,780 frames pumped. Settled heap went **90.05 -> 90.67 MB, +0.62 MB over the entire 21-stop route** (0.031 MB a leg). The mid-drive samples climb 95 -> 100.5 -> 106.8 -> 113.8 and look exactly like a leak, but the settled figure returns to baseline, so that is allocation churn the collector reclaims. Cycle 33 proved the subscriber set does not leak; this closes the heap question behind it.
- **Cycle 68 - S113 parked as not measurable here.** Cache-busted the document URL and still got a useless figure: 11 of 13 resources report `transferSize: 0` because the JS and CSS chunks are content-hashed and already cached from earlier loads, and the browser cache cannot be cleared through this bridge. The 22.6 KB it returned is the **document alone**, not the page, and is recorded as such rather than quoted as a cold-load number. Third measurement this run that I have thrown out rather than dressed up.

- **Cycle 69 - S112, measured: no defect, and my premise was wrong.** The market-research worry was "content buried behind animation". Measured, it is not: **the origin card with the name, role and tagline renders on the very first click** ("Start engine"), the splash itself offers "back to the classic site" for anyone who does not want the drive at all, and the **route map sits in the cockpit and is named on the splash** - three clicks to any of the 21 stops, instantly. The only slow path is the _intended_ one, 2 clicks and 14.1s to EXIT 01, and that is the owner's deliberate design. **No change recommended**; filed as measured rather than acted on, since S112 was explicitly scoped as a measurement.
- **Cycle 69 - measurement caveat worth recording** - My "visible controls" filter (`width > 8 && height > 8`) has been counting the sr-only itinerary's links all along, in this cycle and in cycle 64: `clip: rect(0,0,0,0)` on the parent does **not** zero the children's `getBoundingClientRect`. It did not change cycle 64's conclusion (the two borderless painted boxes were still the only two) or this cycle's numbers, but any future probe that counts "visible" controls by rect size is over-counting and should test the clipped ancestor instead.

- **Cycle 70 - Suggester audit: keyboard driving works.** Never tested in this run - every drive check so far went through `driveToNext()` or pointer events, never the window key handlers. Dispatched real key events: ArrowUp gives throttle 1 and the car accelerates (7.44 m/s, travel advancing), keyup releases to 0, `w` works as the alternate, ArrowDown brakes, and a **full 420m leg completes on keys alone**, parking exactly at travel 420. **No defect.** Two readings looked wrong and are test artifacts, deliberately not filed as findings: the `n` key did not advance the target because the earlier ArrowUp had already advanced it (pulling away only advances from parked), and `index: 0` at travel 420 is React not having committed - the stale-render trap guardrail 64 exists for.
- **Cycle 70 - three new ideas filed**, the sharpest being that **the reduced-motion contract has not been re-verified since cycle 46**, and cycles 57-65 rewrote the road and the cockpit underneath it - the ramp, the drop, the embankment, the two-lane carriageway, and `goTo` now setting `sim.ramp` and `sim.drop`. Reduced motion teleports _through_ `goTo`, so it is directly downstream of code that changed a lot.

- **Cycle 70 - S115: reduced motion still holds, and the specific risk is disproven.** Last verified in cycle 46, since when cycles 57-65 rewrote the road and cockpit under it and `goTo` gained `sim.ramp` / `sim.drop`. Reduced motion teleports _through_ `goTo`, so the worry was that a teleport would land with ramp and drop stranded at the origin's values. It does not: reduced motion jumps to **travel 420 in 2 frames (3 to arrive), with ramp 30.2 and drop -5.5 - both correct for the destination**. The motion-allowed **control genuinely animates** (travel still 0 after 2 frames, speed 0.27, 842 frames to arrive), which is what makes the teleport result mean something rather than proving the harness was dead. **No defect.**
- **Cycle 70 - timing caveat on the reduced-motion probe** - The `matchMedia` patch landed at `readyState: "interactive"`, not the `"loading"` that cycle 29's note describes as the target. It still took effect - `matchMedia` reports true in the patched run and false in the control, and the two behave differently - because framer-motion reads the query at mount. Recorded so a future run does not treat `interactive` as a failed patch, but `loading` remains the safer target.

- **Cycle 71 - S116 and S117: keyboard is sound, both settled in one probe.** S116 was left _unproven_ by cycle 70 rather than passed or failed, because the throttle had already advanced the itinerary and `n` had nothing to do. Retested from a genuinely parked state - `parked: true, target: 0, travel: 0, throttle: 0` - and `n` advances the target 0 -> 1, engages autopilot, and drives to travel 420 in 838 frames. **Pass.** S117: the route map is closed on arrival, opens on `m`, **moves focus into the dialog** (cycle 10's fix still holding), closes on Escape, and the car is still drivable afterwards. **No defect in either.**
- **Cycle 71 - why S116 was worth retesting rather than assuming** - It would have been easy to read cycle 70's `false` as a bug and "fix" a working shortcut, or to wave it through as fine. Neither was justified by the evidence, which is why it was filed as unproven and given its own probe from a controlled starting state.

- **Cycle 72 - Suggester: route map checked, hypothesis did not apply.** Went looking for distance figures in the route map that might have gone stale when `LEG_LENGTH` moved 340 -> 420. All 21 stops list correctly, MILE 0 through EXIT 20 - and the map prints **no distances at all**, so there was nothing that could have drifted. **No defect, and the premise was simply inapplicable.** Recorded as such rather than dressed up as a pass.
- **Cycle 72 - honest state of the run.** Cycles 66, 69, 70, 71 and 72 have now all come back "no defect" on drive mode. That is a real signal, not a failure: the surface has been audited from most angles the environment can reach, and the substantive remaining value is in items that are **parked on the owner** (S107, the wheel over the brake pedal) or **outside this run's write scope** (the classic site's cropped photos, the duplicate canonical, the missing sitemap entry). Flagged in the report so continued self-directed cycles are a deliberate choice rather than momentum.

- **Cycle 73 - re-verified all four parked Needs-human items, 38 cycles after cycle 35 last did.** These are patches the owner would apply blind, so stale line numbers would be my fault, not theirs. **All four still stand.** (1) The classic-site photo patch is exact: `aspect-video` still line 97, `object-cover` still 120, `AnimatePresence` still 104, `handleScreenshotClick` still 61, and `grep` for `setInterval|setTimeout|useEffect` still returns **nothing** - still click-only. (2) `public/sitemap.xml` still has **one** `<loc>` and no `/drive`. (3) `/drive` still ships **two** canonical tags with the homepage one **first**, so crawlers are still told `/drive` is the homepage; `og:url` still doubled. (4) `gosolarindy.energy` still does not resolve.
- **Cycle 73 - one detail reported as it actually came back, not as it was recorded.** The Solar Power Indy repo link failed with a **connection abort** this run, not the clean **404** cycle 56 measured. It is still not reachable, and the control (`github.com/HTJin`, 200) proves the network and GitHub are fine - but "aborted" is not "404", and the ledger now says so rather than repeating the older result.

- **Cycle 74 - Suggester: the toolchain has been telling us three things all run, and every one is out of write scope.** Captured from a real `npm run lint`: an **experimental `scrollRestoration`** flag in `next.config.mjs`, and a **React Hook dependency warning** at `SideNav.jsx:38` (`useCallback` missing `sections`). A third, seen repeatedly in this session's build output, is a **Tailwind safelist pattern `/^apexcharts-.*$/` that matches no classes** - dead config for a charting library the site does not appear to use. All three live in files the guardrails put off-limits (`next.config.mjs`, `tailwind.config.js`, `src/components/**` outside drive), so they are filed for the owner rather than fixed. Cheap to act on, and the missing-dependency one is the only one with any real bug potential.

- **Cycle 75 - end-of-run integrity check: the guardrails held, and it is provable rather than asserted.** Nothing buildable was left (S118 parked on the owner's appetite, S119 an observation, S121 out of write scope), so instead of manufacturing work I verified the branch itself before the owner has to trust 34 commits of it. `git diff --numstat` against `src/content`, `src/components/sections` and `src/lib` returns **nothing** - I never touched a byte of owner content, exactly as the Guardrails block required. The complete set of files changed across the whole run is **nine**, every one of them inside `src/components/drive/**` or `src/styles/drive.module.css` - the declared scope, with no leakage. Build clean, `/drive` at 22.5 kB, and zero lint issues in drive scope.
- **Cycle 75 - the uncommitted files the owner already had are still untouched.** The run opened with modifications sitting in `src/components/sections/*` and `src/content/*` that were not mine. They are still exactly as they were: never staged, never committed, never edited. That was an explicit guardrail ("never `git add -A`") and it is worth confirming out loud at the end rather than assuming.

- **Cycle 76 - Suggester: consolidated everything waiting on the owner into one page.** The ledgers had grown past the point where the actionable parts are findable - the report alone runs to tens of KB and the suggestions ledger holds 120+ entries - so a reader coming to this cold cannot tell what needs them from what is merely recorded. Wrote `overnight-ACTION-REQUIRED.md`: six sections, every item already verified by execution, every file and line reference re-confirmed in cycle 73 rather than remembered. **Nothing new was discovered; the value is entirely in making the existing findings actionable.** That is the honest remaining job when a run reaches the edge of what it is allowed to do alone.

- **Cycle 77 - re-verified my own claim before the owner acts on it.** Cycle 76 put specific pixel figures for the S107 collision into `overnight-ACTION-REQUIRED.md`, but those were measured in **cycle 63** - and cycles 61 and 65 changed the dash and the road afterwards. Asserting a 14-cycle-old measurement in a file written to be acted on is the same failure as quoting a stale line number. Re-measured: the defect **survives both changes** and still collides at both viewports, but the figures were off by a pixel each - **-1px at 1280x1024** (claimed -2) and **-52px at 1024x1180** (claimed -53), with 1920x1080 clearing by 36px. Sub-pixel rounding, not movement. File corrected to what was actually measured, with the provenance noted.
- **Cycle 77 - the general point** - Anything the loop writes for a human to act on has to be re-checked against the code as it stands now, not as it stood when the finding was made. Cycle 73 did this for the parked patches; this does it for the one number the owner is most likely to test first.

- **Cycle 78 - killed a number that was already rotting.** The action file claimed "34 commits" one cycle after it was written; the real figure was 37, and it would have been wrong again by the next cycle. Replaced the count with the two `git` commands that answer the question directly, so the owner can verify the footprint themselves rather than trusting a figure that ages badly. The substantive claims re-checked and unchanged: **nine** source files, all inside `src/components/drive/**` or `src/styles/drive.module.css`, and **zero** diff to `src/content`, `src/components/sections` and `src/lib`.
- **Cycle 78 - stating the run's position honestly rather than burying it.** Cycles 72-78 have produced: one clean audit, an integrity check, a consolidation, a correction to that consolidation, and now a correction to the correction. That is a loop verifying its own verifications. It is still doing the job as specified - the backlog genuinely is dry and everything of consequence is parked on the owner - but the marginal value of another self-directed cycle is now close to zero, and saying so is more useful than manufacturing an eighth. **The loop does not stop itself; this is a signal for the owner, not a decision.**

- **Cycle 78 - the void beside the road: TWO faults, and cycle 65 fixed neither.** The owner had to say it twice. **(1) The ramp had no ground at all.** The verge band is drawn at _mainline_ grade, so the moment the ramp descends the cycle-59 clip removes it - correctly, you cannot see ground above your eye - and nothing replaced it. For the whole descent the ramp was tarmac floating over the background gradient. The embankment only ever filled the wedge between highway and ramp; it never gave the ramp ground of its own. Added a second verge band that follows the ramp down. **(2) The highway had no barrier.** From below, its flat surfaces are correctly invisible, so above the embankment sat an 0.18m lip and then sky - the eye reads a hole where the road is. The shoulder now carries a full-length barrier standing against the sky, like a real embanked highway. Verified **mid-descent** (travel 331, drop -2.51, ramp 13.76) - the state actually being complained about, rather than the endpoints I kept checking.

- **Cycle 79 - owner-directed round on the exit's side profile, three faults in three passes.** Recorded here because the fixes were committed as they were found and the ledger had not caught up. **(1) The ramp had no ground** - the verge band is drawn at mainline grade, so once the ramp descends the cycle-59 clip removes it and nothing replaced it; the ramp was tarmac over background gradient. Added a verge band that follows the ramp down. **(2) The barrier was see-through** - drawn at `withAlpha(..., 0.9)` over a `0.5` cap. "why the hell am I able to see through the road???" Concrete is not 90% opaque; the body is now flat colour with no alpha. **(3) The bank was a 90-degree wall** - `footOf` was `max(TOP + 0.25, rampEdge)`, pinning the foot just outboard of the top for the whole first half of every descent, **with a comment of mine claiming that was "exactly right"**. The run now derives from the drop (`TOP - drop * 2.6`, ~1:2.6), and the 0.92m shoulder parapet came down to a 0.42-0.72m guardrail, since a parapet is the same wall by another name. Vegetation added on the face: deterministic grass tufts every 6m, a flower on one in five, no `Math.random` in the paint loop (guardrail 25).
- **Cycle 79 - the pattern behind all three misses.** Every one of them I "verified" at the endpoints - parked at a stop, or out on the open mainline - where the ramp offset is at maximum or zero. **All three faults lived in the transition between those states.** The owner had to report the same area three times because my check never looked at the thing in motion. Mid-descent is now the state to sample for anything ramp-related.
- **Cycle 79 - standing convention added at the owner's request.** `npm run prettier` after every code change, before committing. Written into a new repo-root `CLAUDE.md` so it binds anyone working here, not just me, along with the two traps this repo keeps producing (building while `next dev` is live clobbers `.next`; Windows PowerShell mangles BOM-less UTF-8 if a source file is round-tripped through the shell).

- **Cycle 80 - a measurement thrown out, and the reason is worth keeping.** Applied cycle 79's own lesson and tested the new vegetation _in motion_: it is gated on there being a slope, so it must be absent on the open mainline and present mid-descent. The probe returned **4,837 flower pixels on the mainline (drop 0) against 1,092 mid-descent** - backwards, and it would have read as a serious bug. It is not: the detector matched **sunset sky**. Amber `(244,208,122)` with a +/-22 tolerance is indistinguishable from the dusk gradient at that point on the route, so the count was mostly sky, and more of it is visible on the mainline than from down in the cutting. **The vegetation gate remains unverified** - not passed, not failed. Filed as S123.
- **Cycle 80 - this is the sixth probe this run that produced a confident wrong number.** The pattern is always the same shape: a colour or geometry test that is not specific enough to separate the thing being measured from the scene around it. Two things have consistently caught them - insisting on a **control** that must come out different, and sanity-checking the _direction_ of the result before believing its magnitude. Here the direction was impossible (more planting where there is no slope), which is what exposed it.

- **Cycle 81 - S123 settled: the vegetation gate is correct.** Cycle 80's probe was thrown out because the flower colours were indistinguishable from sunset sky. This time the flowers were temporarily repainted **pure magenta and cyan** - colours no daylight palette contains - so a hit could only be a flower. Result: **0 marker pixels on the open mainline (drop 0), 7 mid-descent (drop -2.61)**. Nothing is planted where there is no slope, and the control differs, so the zero means something. Marker colours reverted, `git diff --numstat -- src/` back to empty - the experiment left nothing behind.
- **Cycle 81 - what made the difference.** Same question, same page, same harness; the only change was picking a signal the scene could not counterfeit. Cycle 80's detector asked "is this pixel flower-coloured?" in a sunset. This one asked "is this pixel a colour that cannot exist here unless I put it there?" That is the general fix for the six bad probes this run: **do not measure a property the background also has.**

- **Cycle 82 - checked the bank/ramp junction at full drop; no gap.** The slope change created a case I had reasoned about but never looked at: at full drop the bank's run is ~14.3m while the ramp is ~22m away, so the foot stops short and the remainder is carried by the ramp-following verge. Direct observation at travel 420, drop -5.5, ramp 30.2: the ground is continuous from the bank down to the ramp, nothing showing through. **Concern unfounded.**
- **Cycle 82 - and another invalid probe, caught immediately.** I first tried to detect a hole by sampling a "sky" reference 40px above the horizon and looking for that colour below it. The reference came back `(42,33,33)` - dark brown. At full drop the region above the horizon on that side is **the embankment**, not sky, so the zero it produced meant nothing. Same failure as the other six: I sampled a reference without checking it was what I assumed. Cycle 81's rule would have caught it in advance - _do not measure a property the background also has_ - and the corollary is now explicit: **check what your reference actually is before trusting what it proves.**

- **Cycle 83 - refreshed the owner-facing page and confirmed the branch is clean.** Backlog dry and everything of consequence parked, so the useful act was keeping the one page the owner reads accurate rather than generating work. Re-stamped it to cycle 83, recorded that the S107 figures were re-measured in cycle 77 after later dash and road changes, and stated explicitly that **none of the recent side-profile work appears on that page** - the missing ground, the see-through barrier and the 90-degree bank were owner-reported, fixed and verified in cycles 79-82, so their absence is completion rather than oversight. Branch verified: **zero content diffs across `src/`**, nothing uncommitted in drive scope.

- **Cycle 84 - plain smoke test on the real page, and one more selector false alarm.** No harness, no iframe: `/drive` hydrates, canvas present, ignition splash rendering, no horizontal scroll, the `@media print` block still ships. **One reading looked like a serious regression** - the sr-only itinerary measured **26 characters** against the 9,854 recorded in cycle 67, which would have meant the print fix had lost the content it depends on. It had not: `document.querySelector('.sr-only')` returns the **skip link**, and there are three such elements. The itinerary is the second, still exactly **9,854** characters. **Seventh false reading this run, and the fourth caused by a selector grabbing the wrong element** - the same root cause as cycle 62's "cluster 40px off", cycle 64's "29 of 35 controls", cycle 66's "trip summary missing" and cycle 69's over-counted controls. Worth naming as its own rule alongside cycle 81's: **when a selector can match more than one thing, count them before believing the one you got.**

- **Cycle 85 - wrote the run's measurement lessons into the repo instead of leaving them in a journal nobody will read.** Seven measurements across this run looked like defects and were the instrument. They were each recorded where they happened, which is useless to the next person. `CLAUDE.md` now carries them as a short "Measuring the drive scene" section: do not measure a property the background also has; check what your reference actually is; count a selector's matches before trusting the one you got; always run a control that must come out different; sample the transition rather than the endpoints; and check a result's direction before believing its magnitude. Plus the two environment limits that cost the most time - rAF is paused in a hidden tab, and React's scheduler is not driven by rAF so pumping frames does not flush a re-render.
- **Cycle 85 - why this is worth a cycle.** Every one of those rules was paid for with a wrong conclusion that nearly reached the owner, and four of the seven shared a single root cause (a selector matching the wrong element). Leaving that spread across eighty cycles of journal is how the next run repeats it.

- **Cycle 86 - ran the advice I had just written down.** `CLAUDE.md` now tells the next person to format with a scoped `npx prettier --write src/components/drive src/styles/drive.module.css` and to check the blast radius with `git diff --numstat -- src/`. Documentation that does not work is worse than none, so both were executed rather than assumed: the scoped command touches only drive-scope files, and the check returns **0 content diffs**. Verified, not just asserted - which is the same standard the rest of the run was held to, applied to its own output.

- **Cycle 87 - the shipped-work table stopped at cycle 67 and had been wrong for eight cycles.** The owner's three side-profile reports were fixed in cycles 79-82 and verified in 81, but the report's summary table - the thing someone skims to see what changed - still ended before any of it. Added the four rows, including the one that says plainly that a clamp of mine held the bank vertical for the first half of every descent _with a comment claiming it was correct_. A summary that omits the most recently reported defects is worse than no summary, because it reads as completeness.

- **Cycle 87 (cont) - the two owner-facing documents disagreed with each other, and I only noticed by editing near it.** `overnight-ACTION-REQUIRED.md` carries the S107 collision as **1px / 52px**, re-measured in cycle 77. The report still said **2px / 53px** - the cycle-63 figures - because cycle 77 corrected one file and not the other. Whichever the owner read first would have been contradicted by the second. Corrected, with the provenance kept so the change is auditable rather than silent. Also fixed the cycle count, which read 78 at cycle 87.
- **Cycle 87 - the lesson is about correction, not about pixels.** Fixing a number in one place is not fixing it. When a figure has been published in more than one document, correcting it means finding every copy - and the way to avoid the problem is to not publish the same measurement twice. The action page is meant to be the single source for the owner; the report duplicating its figures is what created the divergence.

- **Cycle 88 - cycle 87's divergence was not a one-off; three more figures were duplicated the same way.** Compared the two owner-facing documents for shared measurements: **the leg time, the 4.7-minute route figure and the 52px overlap all appear in both**, so all three could drift apart exactly as the S107 collision did. (The matcher is approximate - it compares literal phrasing, so a figure worded differently in each file reads as "one only". That understates the overlap rather than overstating it.)
- **Cycle 88 - fixed structurally rather than by syncing again.** Re-synchronising the copies would just reset the clock. The report's duplicated leg-length paragraph now **points at the action page instead of restating its numbers**, and says why. `overnight-ACTION-REQUIRED.md` is the single source for anything the owner acts on; the report summarises what happened and defers on figures. That removes the largest duplicated block and the mechanism that produced the contradiction.

- **Cycle 89 - finished cycle 88's job instead of declaring it done.** Cycle 88 removed one duplicated figure from the report and left the other: the S107 section still restated the collision overlaps, which is the precise pattern that produced the cycle-87 contradiction. It now points at `overnight-ACTION-REQUIRED.md` section 1 like the leg-length paragraph does. A partial structural fix is not a structural fix - it just moves which copy goes stale first.
- **Cycle 89 - and I nearly left a mess doing it.** My first edit wrapped the superseded paragraph in an HTML comment "so the divergence is visible in history", which is both unnecessary - git already preserves it - and would have left an unclosed comment swallowing the following section. Deleted it outright instead. **Verified afterwards that the report no longer contains any of the action page's figures**, rather than assuming the edit did what I intended.

- **Cycle 90 - removed the last self-rotting number from the report.** Its header read "87 cycles" at cycle 89 and listed the owner-directed cycles as "57, 58, 59 and 61", omitting 79. I had already learned this lesson once - cycle 78 replaced a stale commit count with the `git` commands that answer it - and then re-introduced the same failure mode in a different field. The count is gone; the header now points at the phase marker in the tasks file, which is updated every cycle by the loop itself and therefore cannot drift.
- **Cycle 90 - the pattern across cycles 78, 87, 88, 89 and 90 is one fault.** A figure copied into a summary is a figure that will be wrong later: the commit count, the S107 overlaps, the leg times, the cycle count. Each was found separately and fixed separately. The rule that would have prevented all five: **a summary should point at the thing that changes, not restate it.**

- **Cycle 91 - the owner-facing summary table did not render as a table.** Blank lines had crept between rows over successive cycles of appending, and in Markdown a blank line **terminates** a table - so the "Shipped since cycle 57" block was rendering as four separate fragments, three of them with no header at all. Anyone reading the report in a Markdown viewer saw broken output, which is exactly the audience that file exists for. Rows were also out of order: 68 sat after 81 because it was appended in a later cycle than the rows above it. Rebuilt as one contiguous table sorted by cycle: 57 through 81, 19 rows, header and separator intact.
- **Cycle 91 - I had been reading it as plain text and never as rendered Markdown.** Every previous cycle inspected this file with grep or by reading raw lines, where a blank line between rows is invisible as a defect. The class of bug is the same one the run kept hitting in the canvas work: **checking the artefact in a form the user never sees.**

- **Cycle 92 - applied cycle 91's lesson instead of only recording it.** Cycle 91 found the report's summary table broken into fragments by blank lines, invisible to every previous raw-text inspection. The obvious follow-up is whether the _other_ owner-facing documents have it too, so all three were checked with a scanner that looks for a blank line sitting between table rows: `overnight-ACTION-REQUIRED.md` (6 table lines - header, separator, four measurement rows), `CLAUDE.md` (no tables) and the report (21 lines, the rebuilt table). **Zero fragmenting blanks anywhere.** The fault was isolated to the one file that had been appended to across dozens of cycles, which is what made it accumulate there and nowhere else.
- **Cycle 92 - a clean result from a check worth running.** Nothing to fix is the correct outcome here; the value was in not assuming cycle 91's fix was the whole problem. The same reflex that kept finding one duplicated figure at a time (cycles 78, 87-90) would have stopped at the single broken table.

- **Cycle 93 - checked the product rather than the paperwork.** Cycles 85-92 were all documentation, so the thing worth confirming was that the actual site had not drifted underneath them. The last commit touching `src/components/drive` or `drive.module.css` is still **`97c1012`**, the slope fix from the owner's third side-profile report - eight cycles of doc work changed no source, which is exactly what should be true but is worth demonstrating rather than assuming. Build clean, `/drive` at 22.8 kB (152 kB first load), drive-scope lint clean.
- **Cycle 93 - and a note on what the loop is now producing.** The last eight cycles found: a stale figure, a contradiction between two documents, three more duplicated figures, a broken Markdown table, and then nothing. Those were real faults in artefacts the owner reads, so fixing them was not waste - but the trend is unmistakable and the remaining backlog is empty of anything the loop may act on. Recorded plainly so the cycle count is not mistaken for progress.

- **Cycle 94 - dispatched the first independent review of the run's own code.** A `code-review` capability became available, and it is the highest-value thing left by a distance: this run changed **nine source files across ~37 commits** and every check on them so far has been **my own verification of my own work**. Cycles 79-82 are the reason that matters - the owner found three separate faults in one area I had already verified twice, including a clamp I had written a comment defending. Running `6c19c99..HEAD` at medium effort. **Result pending; it runs in the background and this note records the dispatch, not an outcome.**

- **Cycle 94 - the independent review found a real bug in my own work, plus five smaller ones.** Verified and fixed the serious one (`f220aa9`): my cycle-89 slope fix dropped the `max` clamp from the bank's foot, so for the first ~26% of every taper the quad crossed itself and painted verge **over the running lanes**, unclipped, at both ends of all 25 stops. Confirmed by execution before fixing (drop -0.86: top 10.6, foot 7.46, carriageway 0-8.2) and after, across the whole taper - 694 frames, zero violations. Also merged the duplicate foot definition that floated the flowers ~1.7m off the slope, and corrected two constants' doc comments that still quoted pre-change values.
- **Cycle 94 - why the review found what I could not.** Every screenshot I took of that area was mid-descent, where the drop is deep enough that the slope run dominates and the bug does not appear. I had written "sample the transition, not the endpoints" into `CLAUDE.md` after the previous round of exactly this - and then sampled the _middle_ of the transition and called it covered. **Three owner reports and one review, all in the same area, all finding things my own verification passed.** That is the strongest argument in this whole run for not being the only checker of your own work.
- **Cycle 94 - and one finding lands on a claim I made.** S124: the print sheet does not hide the dashboard, so a printed page would carry the cockpit over the resume. I shipped that in cycle 67 and verified the rules existed, targeted correctly, and did not change the screen - and said plainly at the time that I could not observe the printed page. The unobservable part is where the defect was.

- **Cycle 95 - S124 fixed: the print sheet named what to hide and missed the dashboard.** The rules covered `canvas` and `[aria-hidden='true']` - the sky, the interior, the road - and missed `#drive-controls`, which carries no `aria-hidden`, along with the arrival panel wrapper, the exit link and the ignition splash. With `.printable` forced to `position: static` those lose their containing block and land on page one over the itinerary. Inverted the rule: hide every direct child **except** the one tagged `printKeep`, which wraps the crawlable itinerary. Naming what to keep is one rule rather than a list, and a new overlay is hidden automatically instead of silently joining the page. Verified by **resolving the shipped selector against the live DOM**: 11 children, 10 hidden, 1 kept, dashboard among the hidden, the 9,854-character itinerary inside the survivor. - `e513d18`
- **Cycle 95 - the pattern worth keeping from this.** I shipped this in cycle 67, verified that the rules existed and targeted valid selectors, and stated plainly that I could not observe the printed page. **The defect was entirely inside the part I had flagged as unverified.** The same shape as the embankment bug the review found a cycle earlier, where the unsampled region of the taper was exactly where it lived. An explicit "not verified" is a placeholder for a bug, not a footnote - the honest disclosure was correct and still left a broken feature shipped.

- **Cycle 96 - cleared the last two review findings.** S125: the `afterprint` handler wrote a hardcoded `'hidden'` while the mount effect three hundred lines up carefully saves and restores the previous value. Now captures on `beforeprint`; verified both paths, including the one the fix exists for - with a foreign value pre-set it restores `clip`, where the old code would have returned `hidden`. S126: a comment block described pedals "positioned out of the flow, pinned to the bottom left" and a second explained why `absolute` must sit on its own node - **neither true for two cycles**, the markup has no `absolute` and the `relative` positioned nothing. Rewritten to match reality, with the abandoned approach named so nobody reinstates it from the old text, and the dead `relative` removed. - `e0d0a24`
- **Cycle 96 - all six review findings now closed**, two of them real defects in shipped features (the embankment painting over the running lanes, the print sheet not hiding the cockpit). The other four were a duplicated calculation, two stale doc comments and a restore that discarded state. **Every one was in code I had written and verified myself**, which is the whole argument for the review having been worth running.

- **Cycle 97 - dispatched a review of the review's own fixes.** Cycles 94-96 changed `RoadCanvas.jsx`, `DriveScene.jsx`, `Dashboard.jsx`, `route.js` and `drive.module.css` in response to the first review - and every one of those changes has only my verification behind it, which is precisely the condition that produced the two defects the first review caught. The embankment clamp and the inverted print rule are both the kind of change that looks obviously correct and has a boundary case. Reviewing `f220aa9~1..HEAD`. **Result pending; this records the dispatch, not an outcome.**

- **Cycle 97 - the second review found a bug I created while fixing the first one.** The cycle-96 `beforeprint` capture was unconditional, so a **double `beforeprint`** (Chrome re-fires on reopening a preview) stored `'visible'` and the following `afterprint` restored _that_ - permanently unlocking scrolling, the precise thing the mount effect exists to prevent. And a **lone `afterprint`** cleared the lock, where the hardcoded `'hidden'` I replaced had been correct. The fix was worse than the original in one direction and newly broken in the other. Now `null` means no print in progress; verified all four paths, including both failure cases. - `65f8394`
- **Cycle 97 - and I walked into my own documented trap.** The BOM at the head of `route.js` came from a PowerShell `Set-Content -Encoding UTF8` I used two cycles ago to edit a comment. `CLAUDE.md` says, in a section I wrote, never to round-trip a source file through PowerShell because of exactly this. Stripped it and swept `src/` - it was the only one.
- **Cycle 97 - two reviews, two rounds, and the second found a defect in the first round's fix.** That is the argument against treating one review pass as sufficient after non-trivial changes: the fix is new code and carries the same risk as the code it replaces. Filed S127 and S128, both low.

- **Cycle 98 - closed the second review's last two findings, and the bank clamp finally matches its own comments.** S127: the `max` floor made the doc's "`min` stops the bank crossing the ramp" false exactly where the floor won - foot 10.85 against a ramp edge of 7.46 - and reinstated a 0.25m vertical face. Gating the clamp on `rampEdge > BANK_TOP` instead of flooring the result fixes both: early in a taper nothing is in the slope's way, so it runs its full length. **Verified both invariants across 694 taper frames: zero inboard feet, zero feet past the ramp.** S128: the `<noscript>` overlay is a grandchild, so the keep-rule could not reach it; tagged and hidden, hook confirmed in the served SSR markup. - `2f4b6dd`
- **Cycle 98 - and one of my own checks misfired again, caught before reporting.** The verification flagged 30 frames with a "near-vertical face" - measuring absolute run under 0.3m. But run is `|drop| * 2.6` by construction, so a 2cm drop gives a 5cm run at a constant 1:2.6 grade. Absolute size is not steepness. **This line of code has now been through three fixes** - inboard foot, vertical floor, gated clamp - which is what a boundary condition costs when each fix is checked only in the region the last bug lived in.

- **Cycle 99 - dispatched a third review, over cycle 98's own fixes.** Not superstition: `bankFoot` has been wrong **three times** (foot inboard of its top, a 0.25m vertical floor, then a doc that contradicted the code), and each fix was verified only in the region the previous bug lived in. Cycle 98 replaced the floor with a _gated_ clamp - new code, new boundary, and both earlier reviews found defects in precisely that shape of change. Reviewing `65f8394..HEAD`. **Result pending; this records the dispatch, not an outcome.**
- **Cycle 99 - the standing pattern, for whoever reads this next.** Across two reviews: eight findings, three of them real defects in shipped features, and one of those was introduced _by a fix for another finding_. Every single one was in code I wrote and verified myself and judged correct. The run's own measurement discipline - controls, direction checks, sampling the transition - caught none of them, because all of it was applied by the same mind that made the mistakes.

- **Cycle 99 - the third review found cycle 98's fix worse than what it replaced, on all three counts.** A 3.72m discontinuity where the gate flipped (visible notch on every ramp, both directions, every stop; the vegetation band jumped with it), an _exactly_ vertical face at the crossover where the old code had 0.25m, and the bank running ~2.8m onto the ramp tarmac for the first quarter of every taper. Three fixes to one line had each traded one artefact for another.
- **Cycle 99 - stopped fixing the line and fixed the shape.** Four failures on one expression meant the expression was never the fault: the ramp began _falling_ while still laterally inside the carriageway, so there were spans where it was below the mainline **and underneath it** - a shape with no ground between the two to slope, which no clamp can express. `dropProgress` now holds the descent until the ramp clears the verge; `bankFoot` collapses to the plain `min` it always wanted to be. `VERGE_WIDTH`/`BANK_TOP_OFFSET` moved into `route.js` because both modules must agree where the verge ends - they were independent numbers for four cycles, and that disagreement is what kept producing malformed banks. Commit `c8dd9d0`.
- **Cycle 99 - verified across the whole taper this time, which is the actual lesson.** 3361 samples 5cm apart through the real `rampAt`/`rampDropAt` (loaded under a Node resolver hook, not reimplemented): max foot step **0.0126m** (was 3.72m), max drop step 0.0049m, min run:fall **2.60:1** - the designed batter everywhere, so the ramp clamp never binds tighter than the slope - **0** samples on tarmac, **0** inverted, depth still -5.500m. Narrow re-checking around the last bug is exactly what let this survive four cycles.
- **Cycle 99 - my first assertion failed and the geometry was right.** The sweep flagged a 0.003m run at the instant the descent begins. But there both run and fall tend to zero together; absolute run says nothing about steepness - the _ratio_ does. Same misread as cycle 98's false "near-vertical" flag, one cycle later. A measurement I wrote can be wrong in the same way twice.

- **Cycle 100 - owner reports the highway's side is still unfixed; stopped the loop to look.** Found first that the browser tab was pointed at `127.0.0.1:3008`, whose server process started **17:33:37**, while the build carrying the cycle-99 fix finished **18:12:59** - 39 minutes later. `next start` loads `.next` at boot, so that server has been serving **pre-fix code**. Served the current build on :3009 to test against something I could name.
- **Cycle 100 - at the stop, the flank is drawn and the ground under it is painted.** Parked at exit 1 (travel 420, ramp 30.2, drop -5.5) the embankment renders as a dark band and the terrain below it samples as a painted gradient (149,97,75 -> 43,32,33 down the column). **No void reproduced at this position.** That is not a clearance for the descent - I never got the camera parked mid-ramp, so the transition itself is still unverified.
- **Cycle 100 - two of my own measurements were worthless before I caught them.** A "sky reference" returned `1,1,0` because it sampled the windshield mask, not sky; and I sampled canvas pixels in screenshot coordinates when the canvas is 3840px wide against a 1568px screenshot - a 2.45x error. Both are the _same_ mistake the repo already documents ("check what your reference actually is"). Writing the rule down did not stop me making it twice in one session.
- **Cycle 100 - I also corrupted the sim while probing it.** Writing `parked`/`target`/`running` directly froze `travel` at whatever I set, and I spent several probes reading a dead simulation before noticing `started: false`. Reloaded and drove it with real key events instead. A harness that silently reports a frozen value is worse than one that errors.

- **Cycle 100 - the void was literal transparency, and the owner's rail remark is what found it.** Froze a mid-ramp frame (drop -4.18) and sampled **alpha**: 0 at y206-212 (x150) and y230-236 (x300) - unpainted canvas, sky element showing through. The shoulder guardrail hangs 0.42m above grade and the bank stopped _at_ grade; nothing painted the 0.42m between. Invisible on the mainline (painted verge behind it), fatal from the ramp (deck clipped, nothing behind it). Every numeric bank check passed while the screen was wrong because **the hole was above the slope I kept measuring.**
- **Cycle 100 - the rail was drawn `() => true`, full length, fixed in world space, while the ramp sweeps past it.** So every exit and entrance drove through a steel barrier. Now breaks at the gore, opening derived from the ramp's own footprint. Commit `1cdd982`.
- **Cycle 100 - then I over-reported the leftovers, and the owner's guess is what exposed it.** I claimed "3 enclosed holes remain". Testing the lane-line hypothesis showed **two were my own detector** counting a lamp halo (alpha 1-7) as paint and calling the sky either side "enclosed", and the **third is above the horizon** - between the bank's silhouette and the horizon line - which is what you correctly see from inside a cut. One reported defect, three false. **A detector that treats a>=1 as opaque will invent holes wherever anything glows.**
- **Cycle 100 - standing lesson: alpha, not colour.** Three cycles of colour-matching missed a hole that one alpha read found instantly. On a transparent canvas over a DOM background, "is anything painted here" is the only question that distinguishes a gap from a dark surface.

- **Cycle 101 - applied my own lesson to my own fix, before anyone had to ask.** S129/S130 were verified at one ramp of one exit. That is exactly the narrow re-checking that kept `bankFoot` broken through four cycles, so I swept the whole domain instead: **13 alpha sweeps**, exits 0→3, both ramp directions, drops 0 to −5.5, open mainline included. **0 transparent samples below the horizon in every one.**
- **Cycle 101 - the zeros only became evidence once the instrument was proven.** The identical grid moved above the horizon returns **784/962 (81.5%)** transparent. Without that, "0 holes" and "the probe is broken" are the same reading - and this run has produced the second of those more than once.
- **Cycle 101 - a silent no-op tried to pass as a clean result.** The first attempt's second leg reported `reachedSamples: 0`: `driveToNext` had been called while the car was already en route, so nothing was measured and nothing was wrong. It was caught only because the harness reports the sim state it measured. **Every measurement should have to say what it looked at, or "nothing" reads as "fine".**
- **Cycle 101 - the control I wanted, I could not run, and said so.** Sweeping the pre-fix build on `:3008` would have proved the detector catches _this specific_ seam, not merely transparency. The fiber walk returns null on that older bundle, so the sim position was unreadable, and a hole count without a position violates this task's own guardrail. Logged as a stated limitation rather than quietly substituting the weaker control.

- **Cycle 102 - the gore was missing, and I found it by looking rather than by reading.** At travel 302 both roads carried edge lines and the wedge between them was bare verge. Every real interchange paints that wedge; it is the most recognisable marking an exit has. `goreRuns` had to be a new primitive because the gore's two sides move differently - fixed on the highway side, sliding on the ramp side - and every `ribbon` helper takes one `follow` flag for both edges. `embankment` already had that shape and lent it. Commit `b137505`.
- **Cycle 102 - a stale server nearly ate the verification, for the second time tonight.** `:3009` was started 18:46 against a build finished 20:04; the earlier `taskkill` silently had not taken, and the port answering 200 looked exactly like success. Caught by comparing process start time to build-manifest mtime **before** measuring. **A server responding is not a server serving your code.**
- **Cycle 102 - filed S132 as an investigation on purpose.** Specks near the horizon are either correct planting on the previous exit's bank or tufts drawn where no bank exists, and at that distance the two are indistinguishable. Naming which it is without measuring would be the exact failure this run keeps paying for.

- **Cycle 103 - found another writer in the same files and stopped rather than merged.** `RoadCanvas`, `CarInterior`, `world.js` and `DriveScene` carry uncommitted changes this loop did not make (a `wall` primitive, `SEGMENTS` 130→170), timestamped 20:17-20:18 - after commit `b137505` and after the 20:04 build. Committing them would attribute another writer's work to this run and could capture a half-saved edit; reverting would destroy work whose intent I cannot see. **Parked as NH-9.** Neither move is the loop's to make.
- **Cycle 103 - checked whether it had already contaminated my evidence, first.** The `:3009` build is 20:04 and the server started 20:05, both before the edits, so cycle 102's S131 verification measured this loop's own code. Worth knowing *before* deciding what the finding means: a concurrent edit invalidates prior measurements only if it predates them.
- **Cycle 103 - the collision is substantive, not cosmetic.** The new `wall` primitive's comment describes the *same* defect this run fixed in `1cdd982` - a zero-width ribbon leaving sky through its joins - and solves it differently. Two correct-looking fixes for one seam is exactly how a regression gets introduced by a fix, which this run has already done once.

- **Cycle 103 - the "underside of the road" was diagnosed by walking a column, not by looking at it.** Mid-descent (drop −3.02), x=200 read: sky, a 6px lit cap, then `43,32,33` **unbroken for ninety pixels** to the ground. One colour from barrier top to embankment foot meant the only edge the eye had was the lit line - and a dark mass with a lit top edge and open sky above it *is* the silhouette of a bridge soffit. The road had a side but no top. Two `rail` calls (barrier above grade, 0.38m deck fascia below it) turned one mass into four materials. Commit `ff3586c`.
- **Cycle 103 - I stated the hypothesis as unverified and then verified it before building.** The previous turn ended with "the reasoning is solid but unverified… the check that settles it is sampling a vertical column". That check is exactly what ran, and it confirmed the mechanism. Worth noting because the opposite - asserting the mechanism and fixing on the strength of it - is what produced four wrong `bankFoot` fixes.
- **Cycle 103 - the owner's method criticism was correct and is now the standing rule.** Stills at rest are the wrong instrument for a 60fps scene; I had been sampling only parked frames. Capturing the descent as a **strip of frames keyed on `drop`** (−0.3 → −5.4) showed in one image what a dozen parked screenshots had missed: the flank is a sliver for the first half of the descent, and the scene is clipped at the frame edge before the road's side is fully in view. **Neither is fixed.**

- **Cycle 103 - "the ground seems transparent" was not transparency at all.** `RoadCanvas.jsx:635` follows the ramp *down* with a 52m-wide swath of terrain, painted across the whole 980m draw distance. The next exit's cut drags the landscape with it and is visible 224m out as a hard-edged sunken terrace. Measured at travel 196 by zooming the vanishing point, not inferred. **Every alpha probe I ran was looking for a hole in something that was solid and simply in the wrong place.**
- **Cycle 103 - shipped nothing, deliberately, and that was the call.** Both cheap fixes are traps with receipts: narrowing the band re-opens the void cycle 65 added it to fix, and a distance gate makes terrain pop as you approach - the exact "unnatural transition" already reported. After four wrong `bankFoot` fixes and two false findings of my own tonight, a fifth speculative patch is worth less than a diagnosis pinned to a line number.
- **Cycle 103 - filed S133 with the guardrail attached, because the fix has a known cost.** Per-segment painting is precisely what `stripes`/`ribbonRuns` exist to avoid: fill each segment separately and you get anti-aliasing seams down the length of the road. Any depth-ordered rewrite has to overlap quads or accumulate runs per surface, and be checked for seams before being called done. Filing the trap with the task is the difference between a plan and a repeat.

- **Cycle 104 - shipped S133 as a bounded depth sort, not a rewrite of the paint loop.** Only the three surfaces that actually live at different heights (grade terrain, ramp-grade terrain, ramp tarmac) paint back-to-front per slice; everything else stays a full-length polygon because markings are coplanar with the carriageway they sit on and cannot occlude one another. Commit `403f7f4`.
- **Cycle 104 - I almost reverted a correct change on a number with no control.** The first frame-rate reading was 84.6ms median (~12fps), which looked like a flat violation of guardrail 2, and I began reverting. Stashing the change and rebuilding gave a **baseline of 83.7ms** - the harness, not the page. The real cost of the change is **+0.9ms, about 1%**. **A number without a control is not a measurement**, and this run has now proved that in both directions: a false positive (7 bogus findings) and nearly a false negative that would have thrown away the right fix.
- **Cycle 104 - caught my own before/after comparison being invalid.** The "before" zoom was a canvas-only snapshot; the "after" was composited with the cockpit, so a dark shape I first read as new geometry was the A-pillar. Recorded the visual result as **qualitative only** rather than dressing it up as a measurement.

- **Cycle 105 - the owner's oldest complaint was real again, and I had re-broken it myself.** The gore opening shipped in `1cdd982` was gone: the barrier had reverted to `rail(0, SEGMENTS, …)`, a solid wall at a fixed 10.9m for the whole length of the road, while the ramp sweeps 6m → 36m across it. It vanished when the continuous side polygon landed and **I committed the regression in `74331f6`/`403f7f4` without noticing.** Restored, and the lit cap gated too - same lateral, so ungated it draws a line across the opening and the gap reads closed. Commit `38d3b6a`.
- **Cycle 105 - my first probe was invalid and I caught it before reporting.** Counting "bright pixels near the shoulder" also counted the ramp's edge lines and rumble strips, which **grow** as the ramp separates - so hits rose from 4 to 22 through the crossing and would have read as "the barrier is still there". That is the repo's own "don't measure a property the background also has" trap, third time this run.
- **Cycle 105 - the magenta technique settled it in one run.** Repainting only the barrier a colour no palette contains: 45,587px at ramp 0.7 → **0px at 2.1 and 3.1** → 48,220px at 7.1. Present before, absent through, present after, matching the predicted 1.55-7.35m window. **The control is inherent** - a probe that reads zero inside and tens of thousands outside cannot be a broken detector. Probe reverted and its absence grepped before committing.
- **Cycle 105 - the lesson worth keeping: a fix is not shipped until something guards it.** This one survived four days of nothing and died silently to an unrelated edit, and no test noticed. Every visual fix in this run has the same exposure.

- **Cycle 106 - measured the descent before believing either side of the complaint.** The geometry is not at fault: 110m of fall for 5.5m is a **5.0% grade** across 65% of the ramp, which is realistic. The *rendering* is: a six-frame strip from 119m out to 48m out shows the ramp curving away and never appearing to descend. **Both halves measured, so the fix aims at the right one** - had I only run the geometry probe I would have closed this as "working as designed".
- **Cycle 106 - the cause is that the land sinks with the road.** A 52m swath of ground follows the ramp down, so nothing in frame holds grade to measure the drop against. A real off-ramp reads as descending precisely because the land beside it does not move.
- **Cycle 106 - filed S135 with its trap attached rather than starting it.** Narrowing that band is only safe now that S133 depth-sorts the graded surfaces, and it **requires a cut wall outside the ramp in the same change** - otherwise grade-level ground beyond the ramp goes above the eyeline, the clip correctly removes it, and the right-hand void returns. That void is the owner's most-repeated complaint. Half of this fix is worse than none of it.
- **Cycle 106 - standing exposure, unaddressed: nothing guards any of this.** The gore opening died silently to an unrelated edit and only surfaced because the owner kept insisting. Every visual fix this run has the same exposure and there is no check that would catch the next one.

- **Cycle 107 - shipped S135 with both halves in one change, which was the whole point of filing the trap.** The shelf narrowed *and* `cutWall` added together; either alone is worse than neither. The descent now reads: at 65m and 48m out the cut wall rises on the right with a curved crest, where the before-strip was flat at every distance. Commit `ce94c3f`.
- **Cycle 107 - a moving reference nearly killed a correct change, for the second cycle running.** The first void sweep reported `control not opaque` at drop ≤ −3.5 and I was ready to call the change a regression. It was my **horizon detector**: it takes the first opaque pixel down a column, and the new cut wall changes what is in that column - the detected horizon moved 317 → 294. Re-run against a **fixed** horizon: 0 holes at every drop, sky control 91-100%. Cycle 106 was the frame-rate baseline; this was the horizon. **Both times the instrument moved and the code was fine.**
- **Cycle 107 - stated the limit rather than letting the win cover it.** At 119-93m out the descent still barely reads, because the drop has not started - it begins 110m from the stop. That is geometry and I did not touch it, so it does not get to ride along with a rendering fix.

- **Cycle 108 - applied the whole-domain rule to my own fresh fix without being told to.** S135 shipped verified at one exit; 17 sweeps later it is verified across legs from 0, 420, 840 and 963, both ramp directions, drops 0 → −4.9: **0 transparent samples in all of them**, sky control 93-100%. Used a **fixed** horizon deliberately - the detected one moved when the cut wall changed its reference column and nearly condemned the change last cycle.
- **Cycle 108 - a CDP timeout did not cost the run anything, because the results lived in a page global.** The three-leg sweep exceeded the 45s budget and the call errored, but every row had already been pushed to `window.__res` and was readable afterwards. **Accumulate into page state rather than the return value** when a probe might outlive its call.
- **Cycle 108 - parked the "cut off" complaint instead of guessing at it (NH-10).** Measured first: the canvas is the full viewport with **no clipping anywhere in its ancestor chain**, so nothing is truncating the scene technically; the framing is `CarInterior`'s pillars, an aperture of about 86% of viewport width, which is roughly a real windscreen. Widening it is a judgment about how much car you want in frame - and it lands in a file still holding NH-9's third-party edits. Not the loop's call to make.

- **Cycle 109 - resolved S132 as a non-defect and found a real one behind it.** The planting gate is `if (drop > -0.35) continue`, so tufts cannot be emitted where there is no bank; the specks near the horizon are correct planting on the approaching exit's bank, in view because the loop spans a full leg ahead. **But reading it surfaced a genuine regression I had caused:** `vegetation` ran before the highway's side polygon, which at a 5.5m drop covers world x 10.9 → 25.2 - exactly the inboard bank - so the face painted over the grass. Measured 0 flower pixels left of camera. Commit `2c2e16f`.
- **Cycle 109 - the cut wall was bare while the bank was grassed.** One landform, two treatments, and my own S135 made it. Both faces now plant through a single `tuft` helper, the outboard one mirrored rather than copied. Before/after at identical camera state: **left 0 → 12, right 6 → 6.**
- **Cycle 109 - three probes failed before one worked, and every failure was mine.** I matched the flowers' *source* `rgba(226,170,196)` when they draw at alpha 0.85 and composite to roughly `(198,149,172)`; then I windowed to `y>=330` to exclude sky, which at the stop excludes most of the visible scene. **Alpha is the right discriminator all along** - the sky is a DOM layer behind a transparent canvas, so any opaque pixel is scene by definition. A null result from an unvalidated probe is not evidence, and I nearly filed "planting does not render" off the back of two of them.

- **Cycle 110 - built the check that would have caught tonight's two silent regressions.** The gore opening and the inboard planting both died to unrelated edits and both surfaced only because the owner kept reporting them. `overnight-scene-probe.md` consolidates the harness I had rebuilt ad-hoc about ten times, with every trap encoded: alpha not colour, **fixed** horizon never a detected one, a control on every invariant, never match a source colour through alpha, sample the transition, and accumulate into page state so a CDP timeout cannot eat the results.
- **Cycle 110 - ran it rather than shipping a stub, and it passes with live controls.** Five states from open mainline to a parked stop: **void 0/1813 everywhere**, sky control 99-100%, planting present on both faces at the stop. That is a baseline a future shift can diff against.
- **Cycle 110 - labelled the weak invariant weak.** Flower counts run 0-12 pixels because only one tuft in five carries a flower; that proves planting renders but **cannot** separate "regressed" from "few in frame". Written into the doc as a caveat with the conditions under which it means anything. A weak check presented as strong is worse than no check - it converts an unknown into a false all-clear, which is exactly how the gore regression survived.
- **Cycle 110 - the gore invariant honestly cannot be measured from a composited frame.** The barrier shares its colour with the ramp edge lines and rumble strips, which grow as the ramp separates, so the naive probe reports it present while it is absent. Documented as needing the magenta build, with the measured pass values recorded so the next person does not have to rediscover them.

- **Cycle 111 - the owner caught a fix that solved the stated problem by breaking the model.** S135 made the descent legible by narrowing the ramp's ground and walling the far side; it worked, and it put a wall to the right of the exit, which no interchange has. **I had verified it against my own two criteria - void closed, slope visible - and both passed.** Neither criterion asked "is this still a highway?", so the check could not fail. Reverted in `9298a98`.
- **Cycle 111 - the model is now written down, because it was the thing I never stated.** There is exactly **one** face in this scene: the highway's embankment, on your left as you descend. The ramp descends to natural ground level; the ground right of it is open. Every fix that has gone wrong here went wrong by inventing a second face.
- **Cycle 111 - the removal was the risky direction and the probe covered it.** Taking the wall out is exactly what could re-open the void, so I ran cycle 110's probe: 0/1813 at four states, sky control 99-100%. The wide ramp-following ground is what covered that void originally and restoring it restores the cover.
- **Cycle 111 - refiled the lost capability as S136 rather than dropping it.** The descent is invisible again, which is a real regression against a real complaint. It is filed with the constraint attached this time - one face, no wall right of the ramp, no distance gating - so the next attempt cannot "solve" it the same way.

- **Cycle 111 - the honest outcome was "this is already fixed", and it took a measurement to know that.** S136 was refiled last cycle as a real regression. Measured on the current build, the highway's silhouette climbs **monotonically** from 0.434 to 0.278 of frame height at x=0.15 across a 4.84m descent - 15.6% of the screen - with the same monotonic rise at x=0.25 and x=0.35. The cue is objectively present.
- **Cycle 111 - it got fixed by work aimed at something else.** The complaint predates the batter, the three-material top and the depth sort. Those gave the highway a *body* with a lit crest, and a body is exactly what rises beside you as you drop. **Had I "fixed" S136 directly this cycle I would have added a second mechanism to a scene that already had one** - which is how the trench happened.
- **Cycle 111 - the control was free and built into the shape of the question.** At drop 0 all three columns read an identical 0.434, the flat horizon. A monotonic series with a flat baseline cannot be a stuck probe.
- **Cycle 111 - refused to score the perceptual half.** Whether it *feels* like a descent is the owner's call; the measurement claims only that the cue exists and grows. Declaring a feeling verified is the failure mode that produced "fixed" three times on the rail.

- **Cycle 112 - closed S113 by measuring twice with different instruments, because one would have lied.** `curl` over the wire gave a worst case of 811.9KB by counting all three fonts; Resource Timing showed the browser never requests `Inter-italic` at all, because `@font-face` fetches lazily and nothing on the page is italic. **Neither method alone is right** - curl over-counts what is declared, Resource Timing under-counts because 13 of 16 resources came from cache. Cold total is **572.6KB**, of which **352.5KB (62%) is two variable fonts**.
- **Cycle 112 - retired a baseline instead of reporting a regression against it.** The cycle-42 figure of 208KB sits within a rounding error of this run's JS+CSS-only number (198.4KB), so it almost certainly excluded fonts by the same error. Quoting it would have produced a headline "2.7x page weight regression" that is pure methodology. **A baseline you cannot reproduce the method for is not a baseline.**
- **Cycle 112 - the night's geometry is not the weight.** Ramps, embankment, gore, barrier, depth sort all live inside 198KB of JS, and `/drive`'s own chunk is 23KB. Filed S137: fonts are the only real win, and it is out of this run's scope so it is filed rather than done.
- **Cycle 112 - explicitly warned the next person off the obvious move.** `Inter-italic` being unused *on /drive* says nothing about the classic site. Deleting it on this evidence would be exactly the kind of confident, locally-verified, globally-wrong change this run has already made twice.

- **Cycle 113 - S112's premise was wrong, and `curl` settled it in one command.** The worry was that content sits behind the animation. The raw document for `?exit=1` contains the stop's text and **all twenty exits** plus the itinerary - server-rendered, no JS required. Crawlers, screen readers and anyone with a link get everything on first byte. Only the *default* landing view costs 23.4s of driving, and that is the owner's chosen design.
- **Cycle 113 - I threw away two of my own numbers before reporting them.** A 41.3s "cold to content" that was 17.4s my own `setTimeout`; and a 12.9s deep-link figure that measured **when my script started**, because `performance.now()` is anchored at navigationStart and the content was already there at the first poll. Both looked like findings. **A timing harness that includes its own latency is measuring itself**, and this is the second cycle running where the instrument was the story.
- **Cycle 113 - the useful reframing: the content was never the problem, the first impression is.** Filed as a measurement, so it stops at the number rather than turning into an unrequested redesign of the thing the owner deliberately built.

- **Cycle 114 - S96 answered: the pedals are safe on touch, and the scary result was my probe.** Four scenarios with `pointerType: 'touch'` - normal release, `pointercancel`, slide-off, and off-coordinate release under capture - all return the throttle to 0. Both branches of the capture logic hold: with capture the retargeted `pointerup` releases; without it (the documented `NotFoundError` path) the browser's leave does.
- **Cycle 114 - I nearly filed "throttle sticks at full after a slide-off", which would have been alarming and wrong.** A hand-dispatched `pointerleave` **does not bubble**, and React synthesises `onPointerLeave` from `pointerout`, so my event never reached the handler. The tell was `hasPointerCapture` returning **false** - the harness could not establish the very mechanism the behaviour depends on, so any conclusion from it was void. **Check that your harness can reproduce the mechanism before trusting what it says about the mechanism.**
- **Cycle 114 - left the untestable half untested rather than inferring it.** Whether a long press raises the iOS touch callout is real-device behaviour; `touch-action: none` and `touch-none select-none` are present but that is code, not evidence. Moved to Awaiting scenario.

- **Cycle 115 - measured S107 rather than acting on a three-cycle-old number.** With one consistent definition the ratio runs 0.281 → 0.227 across desktop viewports, spread **0.054**, and the mechanism is visible in the raw sizes: the wheel scales with its column (302 → 405) while the gauge caps at 92.
- **Cycle 115 - refused to report an improvement, because the metrics differ.** Cycle 62's 0.602-0.831 used a different definition of "cluster". Claiming "spread cut from 0.229 to 0.054" would be exactly the error I retired the 208KB page-weight baseline for, one cycle after doing so.
- **Cycle 115 - did not trust a resize that claimed success.** `resize_window` reported 1440x900 while `innerWidth` stayed 1920, so the whole measurement moved into same-origin iframes sized per target, where the page genuinely sees the viewport. **A tool reporting success is not the same as the effect landing** - the same lesson as the stale servers.
- **Cycle 115 - parked the remainder as NH-11 because what is left is taste.** The fix S107 proposes makes the ratio constant at the cost of cockpit proportions at every desktop size, inside a dash-height budget three prior cycles tuned. The numbers are the deliverable; the aesthetic call is the owner's.
- **Cycle 115 - the in-scope backlog is now dry.** Remaining items are out of scope (S137 fonts), owner decisions (NH-9/10/11), or real-device only (S96's touch callout).

- **Cycle 116 - shipped the debug affordance and, more importantly, proved it is inert.** `?probe=barrier` tints one element magenta so the gore opening can be checked from a URL. The criterion that mattered was not "does it tint" but **"does it change anything else"**: the I1 void sweep is identical with the flag on and off, 0/1813 both ways. A debug affordance that shifted geometry would corrupt the measurement it exists to enable, and would have been very hard to notice.
- **Cycle 116 - the flag is read inside the effect, never during render.** The server has no `location`; branching on it in the render path is precisely the hydration mismatch guardrail 1 exists for, and this run has changed enough of this file that a fresh one would have been easy to miss.
- **Cycle 116 - the payoff is measured, not asserted: 29,699px at ramp 0.6m, 0 through 2.3-4.1m, 41,159px at 7.3m.** Same shape as cycle 105's instrumented build (45,587 → 0 → 48,220) with no source edit. **The invariant that regressed unnoticed now costs one URL to check instead of five steps and a rebuild** - which is the actual defence, since a check nobody runs is not a check.

- **Cycle 117 - shipped `?probe=grass` and refused to claim it solved the thing it did not solve.** It fixes reliability: magenta cannot be confused with terrain, where matching the flowers' alpha-composited colour defeated three probes. It does **not** fix magnitude - 26px at a parked stop, sampling every pixel. The doc now says both, and says to treat *a drop to zero on a side that previously had planting* as the signal, which is exactly the regression that actually occurred.
- **Cycle 117 - the small count was my sampling stride, not the feature.** 6px at stride 2 versus 26px at stride 1, against ~1px strokes. I nearly read the first number as "the tint did not help".
- **Cycle 117 - one of my measurement runs was invalid and is not being reported as data.** The loop's conditions fired at MILE 0, which is itself a stop at drop −5.5, so it recorded the *starting* stop twice instead of a traversal and never captured the mainline case. The inert/opt-in results come from the earlier controlled comparison, not from that run.

- **Cycle 118 - used the tooling instead of building more of it.** Two cycles had gone into probe affordances; the value was in running them. Full sweep on the current build: I1 void 0/1813 across 8 samples and two legs, I2 gore 28,222px → **0 through the crossing** → 42,678px, I3 planting 0/0 on the mainline and 16 left / 0 right at a stop. Everything shipped tonight still holds together.
- **Cycle 118 - designed out the trap that invalidated cycle 117's run.** Each leg now waits for `drop > -1.0` before sampling, so MILE 0 - itself a stop at −5.5 - cannot be recorded as a traversal. Four samples per leg is the proof it worked, where the broken run produced two rows of the same starting stop.
- **Cycle 118 - I3's result is the one-face model showing up in the numbers.** 16 left, 0 right, and 0/0 on open mainline. The right-hand zero is not a missing signal, it is the trench being gone; the mainline zero is a real negative control. **An invariant whose shape encodes the model catches a wrong model, which the trench criteria did not.**

- **Cycle 120 - turned last cycle's single find into a systematic sweep, and it came back clean.** Every numeric claim in the drive sources cross-checks against its constant: `1:2.6` against `SLOPE_RUN`, "a fixed 10.9m" against `CARRIAGEWAY + VERGE + SIDE_OUT`, "10.9 → 25.2" against `10.9 + 5.5 × 2.6`, "five ribbons" against an actual count of five. **No further expired premises.**
- **Cycle 120 - the one real find is a non-defect worth recording.** `SHOULDER_RAIL_FOOT`, the constant the cycle-100 seam fix was built on, has **zero occurrences** - that whole mechanism was replaced by `HIGHWAY_SIDE_TOP` and the continuous side polygon. The invariant still holds and nothing references the dead name, so it is clean replacement, not rot. **A commit message of mine describes a mechanism that no longer exists**, which is a good argument for the journal recording *invariants* rather than implementations.
- **Cycle 120 - what actually distinguishes the survivors is worth naming.** Every comment that came through the sweep is written either as **derived** (`VISIBLE_FROM = LEG_LENGTH`) or as **dated history** ("measured at a 6.5m drop in cycle 58"). The one that failed last cycle asserted a bare current fact - "a leg is 220m" - about a number someone else owned. That is the whole tell.

- **Cycle 121 - did the market research I had twice recorded skipping, and it produced exactly one usable idea.** Most of the comparable-site material was template listicles recommending the stack this project already uses. The transferable finding was the canvas-accessibility and motion-sickness angle: a canvas removes the accessibility the browser provides for free, and continuous first-person forward motion is the canonical vestibular trigger.
- **Cycle 121 - filed S141: reduced motion is honoured, but only the OS can ask for it.** Four call sites respect `prefers-reduced-motion`; in-page controls are **zero**. Someone on a locked-down work laptop who gets motion sick has no lever short of leaving for the classic site. Filed with the mirror of the cycle-9 audio guardrail attached: a persisted "on" must never start motion for someone who asked for stillness.
- **Cycle 121 - raised the "nothing shipped" flag prominently rather than quietly continuing.** Cycles 118-121 produced one comment fix, two clean sweeps and two suggestions. **That is the loop running out of work it is permitted to do, not the loop being stuck** - everything left is an owner decision, out of scope, or a visible change to something the owner designed. Continuing to audit would manufacture activity; the report now says so at the top instead.

- **Cycle 122 - broke the nothing-shipped streak by questioning my own reason for not shipping.** I had filed S141 citing "two visible improvements that had to be reverted". That comparison does not hold: the trench and the buried planting **changed existing geometry the owner had opinions about**. S141 is additive, defaults to the OS preference, and changes nobody's experience unless they ask. **A caution learned from one class of mistake was being applied to a class it does not fit** - which is its own kind of error, and costs real work.
- **Cycle 122 - two probes failed before one worked, and both were the harness again.** `setThrottle` sets `autopilot = false` *before* the branch it was meant to test, so both arms were forced to the same answer by construction. Then I held a `drive` object captured **before** the toggle - a stale closure carrying the old flag - so the control looked inert. Re-reading the fiber after each re-render made it measurable: autopilot true / travel 0 versus autopilot false / travel 420.
- **Cycle 122 - caught an inversion in review that would have shipped a dead button for exactly the users it is for.** `!(was ?? false)` flips the override rather than the effective value; for someone whose OS already requests reduced motion, the override starts `null` and the first click would have done nothing. The people most likely to press it are the people it would have failed.

- **Cycle 123 - went back for a check I owed rather than starting something new.** S141 put a control into the cockpit and I never ran guardrails 3 and 10, which exist precisely because the dash budget was tuned across cycles 12, 25 and 44. Clean: dash **36%** and glass **64%** at 1440×900, no horizontal overflow at any size, control row `scrollWidth === clientWidth` with all five children inside at both 390×844 and 840×386.
- **Cycle 123 - dropped two numbers instead of reporting them.** My dash-detection returned 8.3% of 844 ≈ 70px at phone width, which is impossible for a cockpit with a 210px floor - it had walked up to the wrong container. The 1440×900 figure came from an element that actually spans the viewport, so that one stands and the other two are simply absent. **A measurement I cannot justify is worth less than a gap I admit to.**
- **Cycle 123 - filed S142 rather than quietly enlarging the buttons.** Both toggles are 29×31, which clears WCAG 2.5.8 AA (24×24) and misses the 44px comfort target. It is the **existing** pattern - `MotionToggle` copies `AudioToggle`'s classes - so changing it means changing a control row the owner designed, in a row with zero slack.

- **Cycle 124 - retired a suggestion of my own by measuring what it was worth.** S140 proposed the `1/4 MILE` ladder above 1320ft. Against the real constants it would apply for **17.7m of a 420m approach — 4.2%, or 0.44 to 0.71 seconds** at speed, in exchange for a mid-approach unit switch, which is precisely what the original comment objected to. **The stale premise was the defect; the behaviour it implied is not.** Closed, and recorded as retired so a later pass does not re-propose it.
- **Cycle 124 - ran the linter, which I had never done across ~15 source edits tonight.** One warning, `SideNav.jsx:38`, pre-existing and out of scope. Nothing introduced. Worth noting how late that check came given how much of this file changed.
- **Cycle 124 - the backlog is now genuinely empty of things this loop may do.** Everything left is an owner decision (NH-9/10/11), out of scope (S137 fonts), or filed-and-justified (S142 touch targets, which would change a control row the owner designed). **Retiring work is as much progress as adding it** when the alternative is a backlog nobody can act on.

- **Cycle 125 - audited the owner-facing page instead of the code, and it was 42 cycles stale.** `overnight-ACTION-REQUIRED.md` was last confirmed at cycle 83. Its structural claims all survived re-verification by the commands the page itself publishes - still exactly nine committed source files, `src/content`/`src/lib` still empty, `Projects.jsx` line numbers still landing correctly - which is what the "verify it yourself" section was for.
- **Cycle 125 - the worst staleness was an instruction to redo work I had already done.** The page still listed cold page weight as an unknown and quoted the 208KB baseline. I measured it in cycle 112 (**572.6KB, 62% fonts**) and **retired** that baseline as non-comparable. The owner would have opened DevTools, taken a number, and compared it against a figure that does not mean what it appears to.
- **Cycle 125 - the bigger fault was omission, not staleness.** Five items filed between cycles 100 and 123 were absent from a page whose first line promises "everything below is waiting on you" - including the uncommitted third-party edits. Added as §7, flagged as the only new part for anyone who read the page before.
- **Cycle 125 - corrected my own count from the previous cycle.** NH-9 is **two** uncommitted files, not three: `DriveScene.jsx` was committed with S141 and I kept repeating the old number. **A fact restated from memory across cycles decays exactly like a stale comment.**

- **Cycle 126 - re-ran the action page's two load-bearing claims instead of re-stamping them.** §1 reproduces to the pixel **49 cycles after it was measured and after I added a control to that dash**: −36px at 1920×1080, 1px at 1280×1024, 52px at 1024×1180. §3 reproduces exactly too: `solar-questions` **404**, `gosolarindy.energy` **DNS failure**, `github.com/HTJin` **200**.
- **Cycle 126 - the S141 button did not disturb the wheel/pedal geometry, and now I know rather than assume.** That was a real risk: the overlap is the owner's headline item and I had changed the control row it sits beside.
- **Cycle 126 - the 404-versus-DNS distinction is the part worth keeping.** The repo returns a clean 404 while the domain will not resolve at all, and the account is 200. Those are three different failures with three different causes, and only the owner knows which of theirs is recoverable. Collapsing them to "links broken" would have thrown that away.
- **Cycle 126 - re-verification is now the loop's main remaining product, and that is the honest position.** Everything actionable is parked on the owner; what this loop can still do is make sure the page they will act on is true when they read it.

- **Cycle 127 - found a 50% error in a number the owner was being asked to decide on.** §5 claimed **14.1 s** a leg and **4.7 minutes** for the route. Measured stop to stop over a clean full leg (1260 → 1680, exactly 420m, throttle held): **21.1 s**, so **~7 minutes**. It agrees with an independent 23.4 s for leg 1 in cycle 113. The old figure most likely timed cruising only, excluding the pull-away and the braking into the next stop - both of which the visitor sits through. **The section exists to support "say the word if you want it pulled back", so the figure being half-real made the decision unsound.**
- **Cycle 127 - §2 reproduces exactly:** two canonical tags with the homepage **first**, `og:url` doubled the same way, one `<loc>` in the sitemap. Noted in passing that `grep -c` reported "1" for each because both tags share a line - the count was of *lines*, not tags, and only the `-o` listing tells the truth.
- **Cycle 127 - two measurements were discarded before the good one.** A three-leg run blew the 45s CDP budget, and the retry then timed a **163m** fragment as though it were a leg, because the dead call had left the car mid-leg. Requiring `dist >= 400` and asserting `parked` at the start is what made the third attempt trustworthy.

- **Cycle 128 - challenged "this environment genuinely cannot take these" and two of the three gave ground.** Cold page weight had already fallen in cycle 112; this cycle narrowed two more. **Print:** the `@media print` block is confirmed present in the **served CSS bundle** - a real risk, since a Tailwind purge or the stale webpack cache this repo has produced before could have dropped it silently. **Forced colors:** both pedals compute to `1px solid rgba(0,0,0,0)` in the shipped page, which is precisely the border forced-colors repaints into a visible outline. Neither is the render, but both are the mechanism, and the mechanism is what silently breaks.
- **Cycle 128 - the control did its job again.** The canvas computes a `0px` border and correctly fails the same check, so "both pedals survive" is a real reading rather than a predicate that returns true for everything.
- **Cycle 128 - recorded that frame-rate *regressions* are measurable even though the absolute number is not.** The page said only "unmeasurable". That is true of the absolute figure and false of the useful question: A/B against a rebuilt baseline gave 84.6 ms versus 83.7 ms in cycle 122, which is how a ~1% cost was told apart from a guardrail violation and stopped a correct change being reverted. **"Unmeasurable" was hiding a method that already exists and had already paid for itself.**

- **Cycle 129 - finished §2 with the control that makes a zero mean something.** `handleScreenshotClick` is at line 61 and `Projects.jsx` returns **0** matches for `setInterval|setTimeout|useEffect`. On its own that zero could be a broken pattern; the sibling `ProjectShots.jsx` - the drive component that *does* cycle screenshots - returns **4** with the identical grep. **Every claim on the action page has now been re-run, not re-stamped.**
- **Cycle 129 - rewrote the report, which I had let go stale while auditing everything else.** It still led with "four cycles have shipped nothing", written before S141 shipped in cycle 122. **Auditing other documents while your own status page rots is the same failure the audit exists to catch**, and it is the third time this run a fact of mine decayed by being restated rather than re-checked.
- **Cycle 129 - the honest summary of this stretch: the loop's remaining product is making sure the page the owner acts on is true.** Everything else is parked on a decision. That is stated at the top of the report rather than buried.

- **Cycle 129 - the report was not being rewritten, it was accreting.** 19 sections, 242 lines, carrying **two** "Still open" blocks, **two** "The model" blocks and **two** "Since cycle" blocks, because every "rewrite" prepended a new status section and left the superseded one in place. The same stale fact - NH-9 as three files - appeared **twice**, and I only found it because an edit reported "2 matches".
- **Cycle 129 - consolidated to 6 sections and 100 lines, cutting history rather than losing it.** The journal and log are append-only and are the right home for it; the report is a status page and is worth nothing unless the top of it is true. Left pointers to the two tables anyone might actually want.
- **Cycle 129 - this is the same defect I have been auditing everywhere else, in my own status page.** Cycle 120 swept the code for premises that expired when a constant changed; cycle 125 found the action page 42 cycles stale; and all the while the report was quietly contradicting itself. **A document that is appended to instead of rewritten decays exactly like a comment that is copied instead of derived.**

- **Cycle 130 - audited the file every relief shift reads first, and it was lying about what was left to do.** Of 22 backlog bullets, **14 were finished work still written in the present tense** - including S113 ("cold page weight, measured properly"), which I had closed myself in cycle 112. The Planner mines this section at the start of every cycle, so a future shift would have redone completed work. Each is now marked **✅ CLOSED — do not mine**, with where it was closed, and kept for its rationale trail rather than deleted.
- **Cycle 130 - my first verification of my own fix was wrong, and it is what found the rest.** I filtered bullets with `grep -v "✅"`, but the marker is inserted on the line *above* each bullet, so the filter matched nothing and reported closed items as live. Reading that broken output is what exposed **three more** closed entries I had missed (S132, S133, S140), and then three beyond those (S95, S111, S114). **A verification that fails loudly is worth more than one that passes quietly.**
- **Cycle 130 - the 31 unticked boxes in the cycle-57 section are archive, not work.** Banner added saying so. The closure invariant applies to the *current* cycle's list, and the live state is in Done / Needs human / Backlog - but nothing said that, and a cold-starting shift would have read 31 open tasks.
- **Cycle 130 - three documents in a row have now had the same defect: append instead of rewrite.** The action page (42 cycles stale), the report (two of every section), and now the tasks file (14 finished items presented as pending). **This loop generates status faster than it retires it**, and that is the structural finding of the last six cycles.

- **Cycle 131 - stopped auditing documents and went back to the product.** Six cycles had gone into docs; guardrail 4 (the arrival panel at 390×844) had not been re-checked since `StopCard` was reworked mid-run. It passes on the **longest-prose stop**, the sabbatical entry the owner singled out: panel inside the viewport, no horizontal overflow, Back and Next reachable.
- **Cycle 131 - checked that it *scrolls*, not merely that it *could*.** `scrollHeight > clientHeight` only says overflow exists. Setting `scrollTop` and reading it back - 0 → 400, then to the end with the last child fully visible - is the difference between inspection and execution, and `document.body` failing the identical test is what makes the pass meaningful.
- **Cycle 131 - closed the owner's column complaint at phone width with a measurement.** `columnCount` is unset on every descendant and the prose runs a single 310px column, because `lg:columns-2` does not apply below `lg`. The passage they called unreadable in three columns is one column on a phone.

- **Cycle 132 - verified guardrail 5 and found the thing it was not asking about.** The guardrail's concern is stacked timers; there are none - live carousel intervals never exceed **1** across 5 stop changes clean and 18 earlier. But the one that exists is **never cleared**: created at a project stop, still firing every 4.2s after navigating to a non-project stop and to MILE 0. Filed as S143.
- **Cycle 132 - the effect is correct, so the answer is probably elsewhere.** `ProjectShots` has `return () => clearInterval(timer)` with sensible deps. A cleanup that never runs usually means the component never unmounts, so **S143 is filed as "establish whether it unmounts", not "fix the effect"** - fixing correct code because a symptom points at it is how the trench happened.
- **Cycle 132 - I corrupted my own instrument mid-measurement and caught it in the arithmetic.** Re-patching `setInterval` over my own patch made every creation increment the counter **twice** (3 → 5 for one real creation). `live.size` survived because a Set dedupes; the raw counters did not. **Patch once, filter by the delay you care about, and distrust a counter that disagrees with a set.**
