# Dev journal — 2026-08-05

One line per task as it completes.

- **Cycle 1 · T1** — `route.js` project stops now carry every screenshot as `images[]` (plus `site`), not just `screenshots[0]`. — `8883aaa`
- **Cycle 1 · T2** — New `ProjectShots`: browser-chrome frame at the captures' native 2:1 with `object-contain`, 900ms cross-fade on a 4.2s timer, dots, hover/focus pause, reduced-motion fallback. — `8883aaa`
- **Cycle 1 · T3** — `StopCard` rebuilt: interstate exit shield, media|prose split at `lg`, projects up from the dash, and re-bounded so it clears the rear-view mirror. — `8883aaa`
- **Cycle 1 · T4** — Cockpit rebuilt on real driver geometry: wheel on the driver's axis, hooded binnacle behind it, dash grain, cowl, vents, bonnet, meaningful tell-tales, PRND, terminal centre screen, tilted pedals. — `646b717`
- **Cycle 1 · T4b** — Separate stacked cockpit for phones (cluster strip / screen / controls+pedals in one thumb row); the side-by-side dash overflowed badly at 390px. — `646b717`
- **Cycle 1 · fix** — The per-frame `style.transform` on the wheel was overwriting Tailwind's `-translate-x-1/2`, so the wheel rendered ~180px off its own axis. Centring moved to a wrapper; rotation stays on the svg. — `646b717`
- **Cycle 1 · fix** — A `display: flex` in the CSS module out-ordered Tailwind's `.hidden`, leaking the outboard vents onto the phone layout. `display` moved into the component's utility classes. — `646b717`
- **Cycle 2 · verify** — Chrome came back to the foreground, so all three parked cycle-1 checks were cleared: cold-load hydration (clean), phone title clamp (2 lines, genuinely clamped), reduced-motion carousel (autoplay off, dots still work).
- **Cycle 2 · fix** — Found `line-clamp-2` and `lg:truncate` generating zero CSS: a corrupted `.next` webpack cache was serving stale CSS. `npm run dev:fresh` restored both. Not a Tailwind/config fault.
- **Cycle 2 · T1** — New `daylight.js`: the route now passes time. Golden-hour dusk at MILE 0 → twilight → full night by the toolbox → first light at the destination. Sky, road, verge, haze, lamps and signs all follow it. — `dd4b28b`
- **Cycle 2 · T2** — Exit signs rebuilt as real guide signs: MUTCD exit plaque, twin posts, retroreflective flare + headlight sheen, palette-aware green. — `86d0174`
- **Cycle 2 · T3** — `/drive?exit=11` deep-links an exit; the URL tracks the exit as you travel; junk falls back to MILE 0. — `0d3e493`
- **Cycle 3 · T1** — Trip computer now reads the year, not just miles: 2016 at school through 2025 at StarPlus, then `NOW`. Projects/toolbox/destination have no dates so they never get a fabricated one. — `94eea90`
- **Cycle 3 · fix** — `new Date('YYYY-MM-DD').getFullYear()` reads UTC midnight in local time, so Jan 1st dates report the previous year. The StarPlus UI/UX role ("Jan 2024 - Oct 2024") was showing 2023. Year is now read off the string. — `94eea90`
- **Cycle 3 · T2** — Each leg has its own roadside: guardrail + thinned lamps on the scenic overlook, lights thinned further across the sabbatical. Logic proven by SSR probe; pixels parked as Needs testing. — `c209b57`
- **Cycle 3 · env** — Chrome can no longer reach the dev server on any host/port (curl can). Switched to SSR probes for real-execution verification.
- **Cycle 4 · T1** — Swept every date call site in `src/`. The classic site is clean: `FormattedDate` already pins `timeZone: 'UTC'`. `route.js` was the only affected place and it was fixed in cycle 3. No change needed.
- **Cycle 4 · T2** — Found `/drive` shipping **two** canonical tags, the first pointing at the homepage, plus duplicate `og:url`/`og:title`. Fix belongs in `_app.jsx`, outside write scope → parked as Needs human with an exact patch.
- **Cycle 4 · T3** — The crawlable itinerary now groups stops by leg (h1→h2→h3) and carries a `<time>` year for each of the ten dated stops; the eleven undated ones carry none. — `e799e92`
- **Cycle 5 · T1** — Cockpit now reads properly to screen readers: 10/11 controls named (the 11th self-names), and the gauges, gear selector and trip screen removed from the accessibility tree. — `5fcf996`
- **Cycle 5 · T2** — `/drive` was serving two `<h1>`s; the transient ignition splash is now an `<h2>`. — `5fcf996`
- **Cycle 5 · T3** — `/drive` is absent from `public/sitemap.xml`. Out of write scope → Needs human with an exact patch; it compounds with the cycle-4 canonical defect.
- **Cycle 5 · env** — Chrome still unreachable (3rd cycle). Dev server also hung mid-cycle on a poisoned `.next`; `dev:fresh` cleared it.
- **Cycle 6 · unblock** — The network blocker cleared: Chrome reached a production build on :3008 and drove the route. Cycles 3-5 had been pixel-blind.
- **Cycle 6 · verify** — Cleared the cycle-3 parked item: the overlook guardrail renders as one continuous seam-free ribbon, lamps thin on the overlook and thin further across the sabbatical, no guardrail elsewhere. Frame-rate still unmeasurable (rAF pauses in a background tab).
- **Cycle 6 · false alarm** — `?exit=` looked broken on `next dev`; the real cause was that React never hydrated (body.overflow unset, chunks all 200, no console error). Same commit on a production build worked perfectly. No code defect. Now guardrail 23.
- **Cycle 7 · T1** — Oncoming headlights on the far carriageway: deterministic, pre-allocated, clamped frame delta, palette-tinted. Proven by a controlled pixel diff (12,564 samples changed in the approach path while parked). — `ff1f8d9`
- **Cycle 7 · T2** — Mile markers every half-leg. First pass rendered ~2x1 pixels and was invisible; enlarged and set outboard until they read as green plates distinct from the delineator posts. — `ff1f8d9`
- **Cycle 8 · T1** — A returning visitor is now *offered* their furthest exit on the ignition screen ("Resume · EXIT 13") with "Forget my progress" beside it — never auto-jumped. `?exit=` always wins. Storage is versioned, id-validated, forward-only and fully wrapped. — `4ebe999`
- **Cycle 9 · T1** — Phone-width regression sweep of the three stated priorities: clean, nothing changed. A 26px "overlap" of the arrival panel into the dash was a frozen framer-motion transform, not a layout fault — deliberately not "fixed".
- **Cycle 9 · T2** — Opt-in engine audio: synthesised, off by default, context built only inside the click handler, never persisted, closed on unmount. Verified by spying on the AudioContext constructor. — `655a3ce`
- **Cycle 9 · fix** — The audio toggle lit up even when the browser refused to resume. `enable()` now reports whether audio actually started, so the button can't claim "on" over silence. — `655a3ce`
- **Cycle 10 · T1** — Route map now behaves like the modal it declares: focus moves in on open, Tab/Shift+Tab cycle inside, Escape still closes. It previously left focus outside a dialog holding 23 tabbable elements. — `09fe15d`
- **Cycle 10 · T2** — Found the user's priority (c) only half-fixed: the CLASSIC site still crops every project screenshot (10.1% of width on average) and only cycles on click. Out of write scope → Needs human with an exact patch.
- **Cycle 11 · T1** — Caught a regression this branch introduced in cycle 7: oncoming traffic ignored `prefers-reduced-motion`, sliding toward a parked viewer who had asked for less motion. Now not drawn under the preference. — `eef571b`
- **Cycle 11 · T2** — Swept the whole reduced-motion contract end to end for the first time: traffic, throttle-jumps, panel fade variant and carousel autoplay all hold.
- **Cycle 12 · T1/T2** — Landscape phone: the arrival panel sat 71px behind the dash and glass was down to 45.6%. The dash height was written twice in different units; both now consume one `clamp(190px,36%,48%)`. Overlap 71px→0, glass →50.8%. — `c71b624`
- **Cycle 12 · investigated** — Two alarms dismissed rather than "fixed": no focus-visibility defect (readings came from programmatic focus, which doesn't match `:focus-visible`), and the "control below the viewport" was the sr-only itinerary's links.
- **Cycle 13 · owner request** — Removed the oncoming traffic entirely; it doesn't belong on a site that represents a person. Recorded as standing law so no future cycle re-adds it, and closed S13b (haze) for the same reason. — `d76e0bd`
- **Cycle 13 · owner request** — The car drove astride the centre line, and steering re-centred it there. Camera now sits at `LANE_OFFSET + sim.x` (2.7m = middle of the right lane); `sim.x` is drift *within* the lane, so steering returns you to your lane. — `d76e0bd`
- **Cycle 14 · check** — Verified cycle 13's camera change broke nothing: the exit-sign overlay still lands correctly at the roadside, and the first-run flow (start → hold accelerator → arrive) works end to end.
- **Cycle 14 · T1** — The destination read like any other stop: the email and "back to the classic site" had identical weight. Email is now primary, the exit link quiet, and a trip summary derived entirely from the content (2016 / 9 / 8 / 2.7) sits above them. — `b6f444f`
- **Cycle 15 · T1** — `world.project()` was dead code whose doc claimed it was the single source of truth for all on-screen placement; three sites duplicated it instead. Two now call it; the hot loop stays inlined with a comment saying why. Proven pixel-identical by a stash/rebuild A/B: 0 of 1,992,704 pixels differ. — `e23a9db`
- **Cycle 16 · T1** — The app contradicted itself one click apart: *"Resume · EXIT 13"*, then a route map saying exits 01-12 were never driven. Saved progress is arrival-only and forward-only, so the resume path now restores the whole history behind it — and only the resume path, because a `?exit=` link proves a click, not a drive. — `f0b7c6c`
- **Cycle 16 · recorded, not built** — Parked, the canvas repaints every frame for a provably identical image. Logged as backlog S16a rather than shipped: the win can't be measured with rAF suspended, and an unmeasurable optimisation in a hot path is exactly what guardrail 9 forbids.
- **Cycle 17 · T1** — Twenty-one exits shared one tab title, one bookmark and one history entry — and Next's route announcer was reading that same string assertively on every departure. The title now names the exit, which fixes the tab and the announcement together; the crawler's `<title>` is untouched. — `aafb4d9`
- **Cycle 17 · T2** — The arrival panel was labelled `aria-live` but remounts on every arrival, so it announced nothing and vanished entirely while driving. A permanently mounted `role="status"` region does it properly; verified by node identity across three arrivals. — `aafb4d9`
- **Cycle 18 · T1** — The road was drawn from an eye at 50% of the viewport while the cockpit was laid out around one at 32% — the wheel sat 345px left of the driver's own eyeline. Proved where the vanishing point actually is with a Node probe, corrected the false comment that justified the old framing, and made the middle grid column the steering column so the wheel is centred by construction. The door card fills what that leaves beside you. — `4d24fd9`
- **Cycle 19 · verification** — The window would not resize, so `/drive` was measured inside sized same-origin iframes instead (proved to be real viewports first). The re-centred dash holds at 1100x800 and the phone layout is provably untouched at 390x844 and 844x390. No defect found; the `xl` threshold now carries the arithmetic that produced it. — `72ccf3f`
- **Cycle 20 · T1** — Frame rate measured at last: driving holds 29.9-30.0fps across three samples while *parked* manages only 17-22, so the ~30 ceiling is this environment (28fps for an empty loop) and the drive loop is not the limiting factor.
- **Cycle 20 · T2** — Route map focus return observed end to end: trigger -> dialog panel -> Escape -> back on the trigger. Escape still reaches the global handler.
- **Cycle 20 · T3** — The engine audio was never broken, only untestable: a scripted click grants no user activation. With a real click the context runs, master gain reaches 0.09, and the oscillators climb 43.8 -> 71.6Hz with the revs, the square exactly an octave above, tyre noise building with speed.
- **Cycle 21 · T1** — The road repainted a 3840x1790 canvas every frame while parked to produce an identical image (130 paints / 130 frames, measured). Now skips when travel, lateral position and camera are all unchanged: 0 paints / 140 frames parked, driving untouched, resize still repaints, and 0 of 4,096,000 pixels differ. — `574ff15`
- **Cycle 22 · T1** — The carousel cross-fades with opacity, which does not hide anything from a screen reader - so all four captures were announced at once. Only the visible frame is exposed now, and it follows the cross-fade. — `2f62f6d`
- **Cycle 22 · T2** — The carousel dots were 6x6px targets against a 24x24 minimum, inside a panel that shows on phones. Now 24x24 boxes around unchanged 6px pills, no overlap. Guardrail 69 said the dots must not move; that was unsatisfiable, so it was amended in the open rather than broken quietly. — `2f62f6d`
- **Cycle 23 · T1** — The destination's last two actions were sliced through the middle on a phone (12px hidden). The trip summary now runs four across instead of stacking 2x2, which halves its height; desktop verified unchanged. 375x667 still scrolls and is recorded as a limit, not fixed by cutting content. — `06f6e26`
- **Cycle 23 · T2** — The phone control cluster overflowed its row by ~11px, stranding the audio toggle alone in the corner. Reclaimed from padding, gaps and tracking below sm - one row at 390 and 360, no label or target size changed. — `06f6e26`
- **Cycle 24 · T1** — The bonnet, dash reflection and wipers were pinned at fixed percentages while the dash is a clamp - so below ~528px tall the dash grew over them and on a landscape phone the bonnet vanished entirely (0 of 23px). The dash height is now one `--dash` token read by all three consumers; desktop verified pixel-identical, landscape phone 0 -> 24 of 26px. — `bf95af8`
- **Cycle 25 · T1** — On a landscape phone the trip computer had 20px of box for 54px of content and, being overflow:visible, painted over the controls instead of clipping - the exit line and progress bar were squashed to height 0. Rebalanced the 190px dash budget on short viewports only (52/68/48 from 74/20/62); portrait and desktop measured identical. — `e9cfe30`
- **Cycle 26 · T1** — The ignition splash overflowed short landscape phones for *returning* visitors (the resume controls add ~50px), pushing the only way out of drive mode off the bottom - at 568x320 by 74px. Made it scrollable with the centring moved to an inner `m-auto` wrapper, since a centred flex child that overflows can't be scrolled back to, plus tightened below 430px. — `d2d0484`
- **Cycle 27 · T1** — The route map highlighted the current exit but always opened at MILE 0 - on a landscape phone that is 3 visible rows of 21 with the current one 803px down. It now opens centred on where you are; MILE 0 still opens at the top. A first attempt used `offsetTop` and was 111px out because the scroller isn't positioned - caught by two checks disagreeing, fixed with rect deltas. — `89803a6`
- **Cycle 28 · sweep** — Opened all 21 exits for the first time: every card renders, counters correct, all 28 screenshots load, zero console errors. Clean.
- **Cycle 28 · T1** — The stops with pictures got 925px and the stops with prose got 704px, so the sabbatical hid 40.9% of itself on a 1440x900 laptop. Text stops now get the same width and flow in two columns - fold fixed for two of three and line length improved from ~100 to ~63 characters. A first build made it worse (break-inside-avoid on every child stops columns balancing); the re-sweep caught it. — `6dcdd2e`
- **Cycle 29 · verification** — Re-ran the whole reduced-motion contract against the current build, eight cycles after it was last exercised: carousel frozen, panel opacity-only, Next teleports in 102ms vs 9,887ms with motion allowed, 0 idle repaints. Each checked against a control run so a pass can't mean the harness did nothing. No defect; no code changed.
- **Cycle 30 · T1** — The exit sign's fade-in was dead code: its 420m window is 1.91x the 220m leg, so opacity computed to 1.000 at departure and the sign popped in at full brightness. Window and fade now derive from `LEG_LENGTH`; measured 1.000 -> 0.004 at departure, full by 4.8s of a 9.9s leg, flare starting at 0 instead of 0.227. — `f299914`
- **Cycle 31 · audit** — `Sky.jsx` and `daylight.js` reviewed - shared palette safe, stars deterministic, and the reduced-motion CSS covers all four animations. The last two unaudited drive files are now done.
- **Cycle 31 · T1** — Full night was anchored at 0.92 and labelled 'the toolbox', but the toolbox is 0.95 - so the darkest point fell mid-leg and EXIT 14 measured a brighter moon than EXIT 19. Anchor now derived from the stop; toolbox star/moon 0.850/0.888 -> 1.0/1.0 with the destination untouched. — `f6c267f`
- **Cycle 32 · T1** — Enumerated every numeric constant in `drive/` and asked which must agree with something it cannot see. Three did: Sky re-typed the palette's 360-step quantisation, MARKER_SPACING typed 110 for 'half a leg', and GEAR_RATIOS ended at a literal 42 for MAX_SPEED. All now derived; proven a no-op at 0 of 4,096,000 pixels different across two exits. — `57cd6cc`
- **Cycle 33 · check** — No subscriber leak: 13 per-frame listeners at MILE 0 and still exactly 13 after twelve stop changes, despite `drive` changing identity on every arrival.
- **Cycle 33 · T1** — The sr-only itinerary is clipped but still in the tab order, so Tab spent 25 presses on invisible links - focus painted on clipped-away content - before any visible control. Added a skip link: 25 presses -> 1, with the itinerary untouched for screen readers. Proving it needed real key presses; programmatic focus never matches `:focus` on an unfocused document. — `de9a7da`
- **Cycle 34 · closed** — The cycle-12 'no focus indicator' alarm is settled: 14 real Tab presses show every control with `outline: auto 1px` and `:focus-visible` true, none suppressed.
- **Cycle 34 · T1** — 13 of 14 focus stops were off-screen inside the clipped résumé. Proved by experiment that a `position: fixed` descendant cannot escape the container's clip, so the usual `focus:not-sr-only` reveal is unavailable; added an aria-hidden chip that names the focused link instead. — `ed61397`
- **Cycle 35 · verification** — Re-checked all three parked Needs-human patches against today's files after 25 cycles: all still valid, every line reference confirmed, crop figure corrected 10.1% -> 10.2%, and the duplicate canonical confirmed from the served HTML (the homepage one comes first, so /drive tells crawlers it is the homepage). My own grep briefly suggested Projects.jsx was dead code - the filter excluded the importing line.
- **Cycle 36 · check** — Resizing the window mid-drive is handled: sim kept running to arrival, canvas backing store followed, dash re-laid out, panel fully in view with zero overlap.
- **Cycle 36 · T1** — The exit sign switched from miles to *metres* partway through the approach, in an mph/MI cockpit, and disagreed with the owner's own '1/4 mile' homepage sign. Now feet throughout (720 -> 110 FT measured, single unit), derived from METERS_PER_MILE; no fraction ladder, because below a quarter mile it could never run. — `faf5a43`
- **Cycle 37 · audit** — Checked every year against `src/content/*` in both places they appear. All ten dated stops correct in the cockpit and in the crawlable copy, `<time datetime>` matching visible text throughout, and all eleven undated stops carrying no year at all. EXIT 08 (`2024-01-01`), the entry cycle 4's timezone bug moved to 2023, still reads 2024. No defect.
- **Cycle 38 · T1** — The route map said `aria-modal` but the driving keys stayed live behind it: ArrowUp pulled the car out of the stop with the dialog still up. Keys are now inert while it is open (except Escape and M), without preventDefault so the list still scrolls, and opening it releases any held control - 22 -> 15 mph proves it. — `e06709b`
- **Cycle 38 · finding** — The missing `key` props in `_app.jsx` block four things, not one: the duplicate canonical, a drive-specific social card, `summary_large_image`, and backlog S15's structured data. Recorded rather than shipped.
- **Cycle 39 · T1** — The arrival panel capped at 928px on every screen, leaving 992px of the band empty at 1920 while the sabbatical hid 22.4%. Text stops now widen to 1216px with a third column at `2xl`: hidden 94 -> 0 and the line measure *narrows* 412 -> 363px. A comma-operator slip briefly dropped the picture stops to 704px - caught by measuring, not by the build. — `cb525ea`
- **Cycle 40 · T1** — Project screenshots rendered at 451px - 23.7% of native - on every monitor, while 992px of the band sat unused at 1920. Now 678px (35.7%) where there is vertical room for the taller 2:1 frame; gated on height as well as width because widening at 900 tall introduced 83px of overflow. 2560x900 correctly unchanged. — `cb086c2`
- **Cycle 41 · T1** — Arriving at a project stop downloaded up to 5,306 KB of raw PNG (all frames load - they are stacked in the visible panel). Now through next/image: 131 KB of WebP, ~40x smaller, sized to the frame (470px, or 692px on a big screen). Frame box, cross-fade, contain, dots, aria-hidden and reduced motion all re-measured intact. — `1f9c323`
- **Cycle 42 · sweep** — Re-ran all 21 exits after four cycles of change: 28 images, 0 broken, all through the optimiser, one frame exposed each, zero console errors. `/drive` cold-loads at 208 KB.
- **Cycle 42 · correction** — Cycle 38 said the missing `key` props block four things; measured tag by tag, `next/head` auto-dedupes `<meta name>`, so the drive page's description and twitter:title are already correct. Only the canonical, the `og:` tags and S15's JSON-LD actually need it.
- **Cycle 43 · audit** — Checked whether cycle 41's 40x byte saving cost image quality. It did not: currentSrc is w=1080 into a 451px slot (~2.4x on a DPR-2 display), and the alarming `naturalWidth: 470` is just srcset density-correction, confirmed by re-decoding the URL to 1080x552.
- **Cycle 43 · not changed** — Measured q=75 against near-lossless: mean channel error 1.73/255, 1.5% of pixels over 8. q=90 would improve that to 1.25 for +76% bytes. Left alone - raising it on 'probably looks better' would be a guess.
- **Cycle 44 · checks** — Browser zoom to 200% (720x450) and forced light mode both hold: no overlap, nothing off-screen, scene covers the white body.
- **Cycle 44 · T1** — With JavaScript off, /drive was a dead end - a splash with an inert button and the whole résumé present but clipped to nothing, with no `<noscript>` anywhere in the app. Added one pointing at the classic site, which does render without JS. Verified by disabling scripting for real via an unsandboxed-script iframe; costs the JS path zero bytes of layout and no hydration warnings. — `a779878`
- **Cycle 45 · checks** — Route boundaries audited: junk/out-of-range ?exit values fall back to MILE 0, and progress.js validates stored id against the index. Both clean.
- **Cycle 45 · T1** — At the destination the GO pedal stayed fully lit, depressed under the press and did nothing, while NEXT beside it correctly greyed out. Now disabled and named for the reason. Brake/back/map stay live; exit 10 still drives 0->27 MPH. — `c2338c8`
- **Cycle 45 · T2** — Found while pre-morteming T1: arriving at the destination with focus on NEXT dumped document.activeElement to <body> at t=13.0s - already shipped, not new. Focus now moves to #drive-controls, and only when that is what happened. Throttle released too, since a pedal disabled mid-press never gets its pointerup. — `c2338c8`
- **Cycle 46 · audit** — Four angles, nothing to ship: ignition splash, the MILE 0 mirror (reads "Open road"), reduced motion re-verified against cycle 45's new pedal (teleports at 0 MPH; control drives 5->25 MPH), and wide/short viewport geometry.
- **Cycle 46 · not changed** — On a 2560x1080 a text stop widens to 1216px but a project stop stays 928px. Looks like a bug; is not. Widening makes a text stop SHORTER (340px, 0 hidden scroll at every height) but a picture stop taller: forcing it costs 10px of hidden scroll at 1080 and 150px at 800. Cycle 39's height guard earns its place.
- **Cycle 47 · audit** — Five angles, nothing to ship: the carousel already pauses on hover and focus; browser history is not polluted (history.length unchanged, Back returns to /); drive.module.css is 25 defined / 25 used with no dead rules and no dangling refs; the served HTML carries 21 h3 for 21 stops across 6 legs.
- **Cycle 47 · signal** — Second cycle running with no commit to src/. Logged an exclusion list at the top of the task file so later passes widen the angle instead of re-probing, and noted that the best remaining work (Projects.jsx on the classic site) is parked in Needs human, outside this run's write scope.
- **Cycle 48 · check** — Stop titles are clipped to one line from lg up; swept all 21 stops at 1440x900, 1280x800 and 1024x800. 0 of 21 truncated.
- **Cycle 48 · T1** — EXIT 12's description ended in a raw, unclickable URL. Now a link, with the owner's copy passed through character-for-character (src/lib/projects.js is read-only) and built as React elements, not HTML. Regex tested against trailing punctuation cases that do not ship; 21/21 stops render with zero anchors added elsewhere; the sr-only itinerary deliberately stays plain so no tab stop returns to it. — `4a54a2e`
- **Cycle 49 · T1** — Swept all 21 panels for hidden content. EXIT 4 (the sabbatical, 2,023 chars - the longest entry on the resume) was the only stop with anything below the fold: 22% hidden at 1440x900, 34% at 1280x800, but 0 at 1920. Cycle 39's wide layout was gated at 2xl; moved it and the third column to xl. Zero regressions across all 21 stops at both sizes; total hidden at 1440x900 is now 0; the measure narrows 412px -> 359-363px. — `9a2a721`
- **Cycle 50 · audit** — Cycle 49's change re-checked where wide meets short (never tested there): 1366x768 hides 52px vs 136px before, 1280x720 76px vs 160px. No regression.
- **Cycle 50 · T1** — The panel band's top was a flat 14% while the mirror's drop is a fixed 50px, so they crossed below 769px tall: gap 0 at 1366x768, -3 at 1280x720, -25 on a landscape phone, with the chip visibly sliced by the panel border. The mirror cannot move (headliner is right above it), so the floor went on the panel: max(14%,calc(7.5%+58px)). All 21 stops now clear by >=8px; tall viewports byte-identical. Costs 8-11px of reading room at 720-768, which is recorded as a cost. — `6544a7a`
- **Cycle 51 · audit** — Cycle 50's 58px mirror floor re-checked on phones, where a long mirror title could have wrapped and broken it: the chip stays 38px at all 21 stops at both 390x844 and 844x390 (titles truncate), min gap +16 and +8, no negatives. Route map geometry also clean at every size.
- **Cycle 51 · T1** — The route map marked the current exit in colour alone - no aria-current anywhere - so a screen reader met 21 near-identical buttons. Added aria-current="location" (ARIA's token for a place on a map). Exactly one row marked at exits 0/12/20, it is the same element as the visual highlight, and the styling is unchanged. — `d88f5cd`
- **Cycle 52 · audit** — Hunted the whole "state in colour alone" class after cycle 51's find: clean everywhere else. Canvas, sky, interior, exit sign, cluster, gear block and trip computer are all deliberately aria-hidden with the meaning carried in text elsewhere. Canvas DPR handling also correct.
- **Cycle 52 · limitation** — Frame rate is NOT measurable in this environment. Driving sampled 16.1 fps, which looks catastrophic - but a bare blank page runs at 15.0 fps and parked drive mode at 15.2, with the window foregrounded. Drive mode is at the empty-page ceiling. Recorded at the top of the task file so no later cycle reports the harness as a regression.
- **Cycle 53 · T1** — No visibilitychange handling anywhere: disable() only ramps gain, nothing suspends, and update() rides on rAF - so a hidden tab left the oscillators holding their last revs with the gain still up. The context now suspends on hide and resumes on show, gated on the sound being on. Verified by patching AudioContext.prototype and driving a real visibilitychange: suspend on hide, resume on show, zero contexts and zero calls when sound was never enabled, listener removed when toggled off. Whether Chrome suspends hidden tabs itself could NOT be tested here - the fix is a no-op if it does. — `78d7f0f`
- **Cycle 54 · T1** — My own regression from cycle 53: the visibility handler ignored unpause()'s return value, so a refused resume left the toggle claiming sound was on over a suspended context. Proved it by patching AudioContext.prototype.resume to reject. Now only `false` turns the toggle off (undefined = no engine). Verified refusal, recovery, success and sound-never-on. Cycle 53 passed because the happy path passes either way. — `e5fce6a`
- **Cycle 55 · audit** — Applied cycle 54's lesson as a lens: hunted unhandled failure paths. router.replace has no catch and Next rejects on cancelled route changes, but 14 rapid Back/Next clicks produced zero errors, zero unhandled rejections and a coherent state. Chaos during motion also clean, and the car recovers - stopping between exits with no panel is correct, not a desync (my check was wrong, not the page).
- **Cycle 56 · audit** — Checked every outbound link for the first time. resume.pdf is genuine (decompressed and read; matches experience.js), 18 of 21 URLs resolve, LinkedIn's 999 is anti-bot not dead.
- **Cycle 56 · Needs human** — Solar Power Indy (EXIT 11) has TWO dead buttons: gosolarindy.energy is NXDOMAIN (nslookup 'Non-existent domain', curl exit 6, www and http variants same, control vercel.com 200) and github.com/HTJin/solar-questions is 404 in 0.39s while every other HTJin repo returns 200. Both live in src/lib/projects.js, which is read-only - parked, not guessed at.
- **Cycle 57 · environment** — The session opened on six orphaned `next dev` servers (3111-3116) plus a `next start`, all from the previous session and all sharing `.next`: guardrail 6 running six times over, with `/drive` serving a flat **500**. Killed them, wiped `.next`, rebuilt clean. Recorded at the top of cycle 57 so a later pass checks for orphans before suspecting the code.
- **Cycle 57 · T1** — The owner asked for a real highway, and the road was not one: a **dashed** centre line at x=0 is the marking for a road you may overtake into oncoming traffic on, and every stop was just a point on it the car halted at. Now `ROAD_HALF` is `CARRIAGEWAY` (the old name claimed "from the centre line"), the left lane is a **median with a barrier** and an empty carriageway beyond it, and every stop is a real interchange — `rampAt(s)` peels a deceleration ramp off the mainline over the 88m before each stop and merges an acceleration ramp back over the 88m after, one smoothstepped shape for both. `RAMP_LENGTH` is derived from `LEG_LENGTH`, because the entrance ramp out of one exit has to finish before the exit ramp into the next one starts. The ramp is added **outside `sim.x`** in `cameraX()`, so the owner's lane-centring rule survives. Proven at a stop by pixels — mainline, edge line, gore, ramp edge line, ramp. The taper **in motion** is NOT proven: the Chrome window is minimised, rAF is paused (guardrail 24 — the first probe hung CDP the full 45s), and every position reachable without the sim loop is a stop, where the ramp is at full offset by definition. Left in Needs testing. — `a511ce0`

- **Cycle 58 - verification** - Cycle 57's ramp taper is proven in motion after all. Guardrail 24 had been treated as "wait for a human to un-minimise Chrome"; it is not. Patching `requestAnimationFrame` inside a same-origin iframe *before hydration* (cycle 29's timing) turns the drive loop into something I can crank by hand at 16.7ms a frame, and the live sim comes off the canvas's React fiber in one hop. Drove 557 frames, zero errors: `ramp` is exactly 0 from travel 308 to 351.6 - the predicted mainline window to the metre - 8.2 at both stops, symmetric about the leg, largest slope discontinuity 0.004. Recorded as a reusable probe; it retires a standing excuse.
- **Cycle 58 - T1** - The dash was symmetric at xl, so the wheel sat on the screen centre line with a 503px empty slab beside the driver. Cycle 18 had centred it on sound geometry - the camera is the driver's eye, so what is in front of the driver lands in the middle of the image - but that renders a car with the driver sitting in the middle of it, and the owner said so twice. Columns now 0.5fr / wheel / 1.5fr: wheel 358px left of centre at 1920 down to 221 at 1280. `lg` still measures the exact 153px cycle 18 recorded, which is how I know I did not disturb it. Guardrail 50 amended in the open. - `f1c5b4f`
- **Cycle 58 - T2/T3** - The pedals took three goes: far right (wrong side of the car), inboard as a flow child (shoved the HUD 330px right), and finally pinned out of the flow at the bottom left of the console column - right of the wheel, costing the console nothing. Two things only measurement caught: `.footwell` sets `position: relative` for its shadow and beats Tailwind's `absolute` on source order, silently dropping the pedals at the top of the dash; and below xl the centred button row ran under the pedals, GO overlapping "Back" at 1100x800. Overlap is now asserted pairwise at five widths. - `6d2e3aa`, `5ee5782`
- **Cycle 58 - T4/T5** - The highway was a single 5.5m lane each way and the exit cleared it by 2.7m. Now two lanes with a broken lane line drawn from `LANES`, the ramp 14m clear, `LEG_LENGTH` 220 -> 340 so the ramp could grow 88 -> 136m, and the exit descends: `rampDropAt` shares one `rampProgress` with `rampAt`, proven by `drop / ramp` holding constant to 2e-17 over 617 frames. The drop is capped at `CAM_HEIGHT * 0.85` and the cap is a renderer limit, not taste - at 6.5m the entire mainline lifted above the horizon and painted across the sky, because flat ribbons with no depth buffer cannot show an embankment. Filed S102 rather than bodge it. - `694c4f6`
- **Cycle 58 - T6** - Cycles 39/49 measured multi-column text stops as a win and were right about *items*; prose is the opposite, and the sabbatical - the longest and most personal entry on the resume - was set in three short columns you had to track bottom-to-top every few lines. Split now by content rather than width: paragraphs-only stops get one column with the measure bounded in `ch`. - `4195b71`
- **Cycle 58 - not a defect** - The "gap spilling out the road on the right side" was my own harness: a 1440px iframe overlaid on the live 1920px page, with the page showing through beside it. Removed, reloaded, re-measured - full-width dash, no horizontal scroll. Nothing changed, because nothing was wrong.
- **Cycle 58 - self-inflicted** - Mangled `useDrive.js` with a PowerShell `Get-Content`/`Set-Content` round-trip: WinPS 5.1 reads as ANSI, so every em-dash came back as mojibake. Caught it in the tool's own diff, reverted with `git checkout`, redid the edit properly. Source files get edited with the editor, never round-tripped through the shell.

- **Cycle 59 - T1** - S102, the embankment. The cap on the ramp's descent came off by fixing its cause: a horizontal surface above your eye cannot be seen, a vertical face can. So the flat road surfaces are drawn inside a clip to below the eyeline while standing objects - lamp masts, the median barrier, the embankment face - stay outside it, and a new `embankment()` fills the gap with its top edge at `point.y` and its bottom at `point.yRamp`, the same two values the two roads are painted from. Drop 1.15 -> 3m. **I tried 7.5m first and rejected it by looking at it:** with the camera ~6m below and ~17m to the side of the highway the embankment's near field is off-screen left and its polygon sweeps in as a black wedge over the sky - this projection running out of road, not a constant to nudge. Filed S104 for the real 3D clipping that would be needed. Crest case re-checked at travel 150.6 per my own pre-mortem, since a baseline measurement had shown 26,683 road pixels already painting above the eyeline before I touched anything: mainline correct, no artefacts. Three legs pumped, arrivals exact at 340/680/1020, `drop / ramp` constant to 5.6e-17. - `8264530`


- **Cycle 60 - T1** - Drove the pedals with touch for the first time and found two real defects, both in `onPointerDown`. **A disabled button is not inert:** Chrome dispatches pointer events to it, so pressing the greyed-out GO at the destination ran the handler and set the throttle. I did not infer that from synthetic dispatch - I settled it with a **real click** on a disabled button next to an enabled control that proved the click landed. And **`setPointerCapture` could eat the press**: `?.` guards a missing method, never a throw, and when it threw, `onPress()` never ran and the pedal did nothing at all. Both fixed; re-verified across seven cases with zero uncaught errors, including that the brake stays live at the destination so cycle 45's contract is intact. - `ae31e76`
- **Cycle 60 - method** - Two of my first-pass "failures" were the harness, not the product, and saying so mattered as much as the fixes. React synthesises `onPointerLeave` from `pointerout`, so dispatching a raw `pointerleave` proved nothing; and React's scheduler is not driven by rAF, so pumping frames does not flush a state update - the first destination check read `disabled: false` purely because the re-render had not happened. With a real wait it reads `true`. A harness that lies looks exactly like a product that is broken.
- **Cycle 60 - non-finding** - Chased mojibake in `Dashboard.jsx` that was not there: Windows PowerShell 5.1 reads BOM-less UTF-8 as ANSI, so `Get-Content` *displays* em-dashes as garbage in files the editor wrote correctly. Files PowerShell itself writes carry a BOM and read back clean, which is why the earlier check passed. Verify encoding with the editor, not the shell.


- **Cycle 61 - T1** - Measured the centre display before changing it: **720 x 116, an aspect ratio of 6.2:1**. Researched what cars actually use - production centre displays are ~10-12.3 inches on a **16:9** panel (~1.78:1), and the pillar-to-pillar outliers like the 56-inch Hyperscreen are several displays side by side rather than one strip, so they are not a precedent. The box is now `aspect-video` taking its width from its height: 1.77-1.78:1 at every width tested. Two knock-ons taken deliberately: the centre stack is capped at 24rem, because a stack should not be wider than the display it houses, and the footwell goes back into normal flow - safe only because the stack is capped, since an in-flow footwell beside an *uncapped* stack is exactly what pushed the console across the cabin in cycle 58. - `664def6`
- **Cycle 61 - T2** - The exit panel was 76rem and three columns at xl. Cycle 49 added that third column and measured it as a win on both axes - nothing hidden, narrower measure - which was measuring the wrong thing: a 1216px panel three columns wide is a broadsheet, and it reads as one. Card down to 60rem, two columns maximum, measure now 433px or about 57 characters. - `664def6`


- **Cycle 62 - audit** - Checked the rest of the cockpit's proportions after cycle 61 fixed the "elongated hud", since the owner said "certain parts", plural. **Two expected defects were not defects.** The cluster is exactly centred on the wheel axis (offset 0 at both 1920x1080 and 1440x900) - my first reading said 40px off, and that was me measuring the two round dials while ignoring the row's third child, the gear block. And the gauges overflowing their flex row by ~9px a side is invisible, because the element that paints the binnacle is a wider parent. Both recorded so a later pass does not "fix" either.
- **Cycle 62 - finding, not shipped** - The cluster and the wheel are sized by unrelated rules: gauges in `vh`, wheel from a `vw`-based column. Measured across six viewports the ratio between them runs **0.602 to 0.831**, a spread of 0.229 - the cluster is 38% larger relative to the wheel at one window size than another, where a real car has one piece of hardware. That is the cycle-32 class of fault. **Deliberately not fixed here:** guardrail 52 forbids quietly redesigning the cockpit, and the obvious fix lands in the dash height budget that cycles 12, 25 and 44 tuned for short and landscape-phone viewports, with `aspect-square` fighting any height cap. Filed as S107 with the numbers and a safe shape for the fix.

